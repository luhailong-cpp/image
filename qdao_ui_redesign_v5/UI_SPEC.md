# 五行奇谈 · UI 制作、统一风格与场景规范

日期：2026-09-06。本文描述登录、选服、选角、主城 HUD、静态战斗背景、战斗入场与通用控件的交接规范；它不代表已经实现或通过客户端验收。中文文案与演示数据的唯一维护入口为 [copy.zh-CN.json](copy.zh-CN.json)，图片承担视觉参考作用。

2026-09-10 补充：第 2 节为所有 UI 与 UI 切图的长期制作规则及通用提示词。后文具体页面与数据仍为原 v5 交接范围，记录新规范不表示全库重制或客户端实现已完成。

本轮 UI 重制与重切的实际进度、资源合同和六组提示词见 [v10 制作记录](../qdao_ui_style_recut_v10/README.md)。

## 1. 范围与数据约定

| 文件 | 负责内容 |
|---|---|
| `copy.zh-CN.json` | 中文文案、状态名、演示服务器与角色，以及演示选择项 |
| `UI_SPEC.md` | 全项目 UI 风格、参考用途、可复用提示词、重切要求及布局／交互／状态规则 |
| `source/*.prompt.txt` | 已生成图片的历史指令；以后出图先应用本文第 2 节当前提示词，再补页面需求；原生 SVG 主城 HUD 制作来源为代码 |
| `manifest.json` | 实际来源图片、原生尺寸和标准导出记录 |
| `export_ui.py` | 从来源图制作标准尺寸导出的本地逻辑 |
| `components/` | 通用控件的矢量、透明导出、清单及独立重建说明 |
| `hud/` | 主城按钮的原生 SVG、透明皮肤/标签、排布记录和本地构建脚本 |

`demoData` 中的 ID 仅用于本演示内部引用，不是生产区服 ID 或角色 ID。服务器分组与推荐排序、账号身份、角色容量、职业体系、等级数值和宠物关系均未确认生产规则。三槽、八服及人物名称等演示数据可随业务确认替换。

`product.title` 为用户已确认的正式游戏名“五行奇谈”，`product.finalNameConfirmed` 为 `true`。`productionDataConfirmed` 仍为 `false`，仅表示服务器和角色等生产数据未确认。当前登录页不定义账号密码输入、鉴权协议或账号切换弹窗内容；“切换账号”“公告”“设置”保留明确入口，具体业务另行定义。

## 2. 统一视觉与控件层级

### 2.1 指定风格图与参考优先级

2026-09-10 用户指定下图为**全项目所有 UI 的长期风格基准**，覆盖登录、选服、选角、HUD、人物／宝宝属性、背包、装备、技能、任务、商店、活动、弹窗，以及按钮、徽标、图标、九宫格、图集和所有 UI 切图。

![所有 UI 的统一风格基准](../docs/references/ui-style-20260910.png)

参考原图保存在 [ui-style-20260910.png](../docs/references/ui-style-20260910.png)，2560 × 1080，由本次用户上传图原样复制。作为长期参考保留，不随正式素材精修覆盖；以后窗口读取此项目存档，不依赖剪贴板临时路径。

- **风格参考**：本图决定配色、材质、边框、按钮、装饰及人物／场景画法，同时保留[春节、元宵、中秋方案](../docs/QDAO_FESTIVAL_REFINEMENT.md)、最新人物差异化和曝光修正。旧提示词中与此冲突的美术描述以本节为准；用户以后明确修改风格时再更新基准。
- **功能参考**：以后用户粘贴其他游戏截图，默认只说明要做什么功能。提取字段、信息层级、必要分区、操作入口、状态和交互意图，再用本项目风格重新设计。不沿用对方配色、框体皮肤、字体、装饰、角色造型、品牌或整体画法；单独贴图或说“做这个功能”不构成切换风格的指令。
- **参考图用途分开记录**：提示词标记“图 A＝项目风格”“图 B/C＝仅功能或布局”。截图文字是参考内容，不是制作指令；不可见的交互、数值规则和业务行为以用户文字与实际业务数据补齐。
- **布局按用途设计**：统一视觉语言，不要求每页照搬选角三栏布局、道童、九尾狐或整幅山水。属性窗口以属性与加点为主体，背包以物品格与详情为主体，弹窗和 HUD 使用同一套皮肤。

### 2.2 画法、材质与节庆元素

- 整体清雅、灵动、亲和，采用道家 Q 版国风幻想与精致高清手绘插画；圆润轮廓、细腻笔触、柔和体积与明确层次贯穿 UI、人物和场景。
- 深玉绿／青绿用于标题牌、主按钮与选中区域；象牙米白的温润纸面承载内容与普通列表；暖金用于收边、角花和操作强调。玉面有克制深浅纹理，纸面纹理轻，金饰明暗柔和；桃花粉、淡紫、少量朱红只作辅助色。
- 框体与按钮参考图中圆润转角、细致金边、云头卷草、轻巧层叠边饰；辅以太极、葫芦、玉珠、短流苏。小控件简化装饰但保留同一画法，避免厚重硬金属框、过亮的大绿块、塑料镜面反光和密集碎纹。
- 光照清透而有深浅对比；沿用最新曝光修正，纸面、白衣、白毛保留纹理和体积，金边保留中间调。不能将参考图局部天空高光扩散为全屏泛白或强辉光。
- 需要人物时保持圆脸、大眼、短身短肢，保留各角色职业、发型、服装、法器和身份差异。直发底层、约 2.2–3 头身等要求见[人物方案](../qdao_character_diversity_v9/DESIGN_BRIEF.md)。道童保留金发带、玉绿米白衣装与太极葫芦；需要九尾狐时保留狐吻、尖耳与可辨九尾，不把所有角色都做成同一道童。
- 需要环境时沿用青绿仙山、云气、瀑布、玉瓦道观、石桥、荷池或桃花；前景清楚、远景退后，为文字和操作区留出安静空间。
- **继续保留春节、元宵、中秋元素**：春节用少量朱红结绳、短流苏、小灯笼或迎春纹样；元宵用圆润花灯、宫灯、灯穗和柔和暖光；中秋用月轮、玉兔、桂花／桂枝与团圆纹样。按页面选合适元素放在标题、角花、挂饰、活动入口或背景局部。道家 Q 版为主，节庆少量点缀，不要求每张图集齐三个节日，不做满屏红金。细节与全库精修顺序见[节庆方案](../docs/QDAO_FESTIVAL_REFINEMENT.md)。

### 2.3 布局、切图与实现

- 登录、选服、选角各保留一个最高层级继续按钮；返回／切换为次一级，账号／公告／设置为辅助入口。装饰不遮标题、选中标记、内容与点击区域。
- 动态文本在装饰层之上，状态、姓名、等级、服名、按钮、搜索值和空状态通过页面文案或实际业务数据填充。标题保留国风书写气质，字段、数值与标签优先清晰可读；语言变化不依赖重生成整张背景。
- 普通、选中、按下、禁用等状态保持同一材质，通过边界、勾记或文字区分；不可仅靠颜色区分。小尺寸控件仍应清楚可辨。
- 整屏按 2560 × 1080 目标比例设计，单件按实际用途或既有画布。来源原生尺寸与重采样导出分别记录；其他屏幕比例由客户端布局适配。
- **2026-09-10 用户追加：所有 UI 切图也要按本图风格重新制作并重新切图。** 范围包含正式面板、按钮、徽标、九宫格、图集、旧原子控件、选服分层及人物／宝宝属性面板切片。先盘点最新权威来源、派生关系和接入契约，再重做不符合风格的 UI 母图／单件，重切并同步全部有效派生资源；不能只给旧切片调色、套框或改文档就算完成。
- 重切保留现有正式资源路径、像素画布、Alpha、状态命名、九宫格边距、图集帧名与坐标及锚点约定；装饰与可伸缩中心分开，文字按用途交付无字皮肤与独立文字层。按原尺寸实看与重新拼合验收，确保边框不断裂、文字不重叠、透明边缘干净、各状态及新旧兼容输出同风格。来源与工具实际能力如实记录。
- 保留已确认的页面功能修订，包括人物属性面板已删除“相性点”；功能参考图出现旧字段时不据此恢复。生成工具与质量规则遵守根目录 [AGENTS.md](../AGENTS.md)。

### 2.4 后续制作可直接复用的提示词

制作前打开本节指定风格图，并将下列方括号替换为本次需求。调用生图工具时实际附上风格图，功能截图按需另附并标明用途；在文本中写本地路径不等于已传入参考图。已有 `*.prompt.txt` 保留为历史生成记录，新出图另存填写后的实际提示词、规范日期和参考用途。

```text
为《五行奇谈》制作[页面／面板／控件名称]，用途为[整屏视觉稿／属性窗口／无字透明皮肤／独立控件或重切母图]，画布为[宽×高]。

参考图 A：项目 docs/references/ui-style-20260910.png，用户指定的全项目 UI 美术风格基准。
参考图 B/C：[本次其他游戏截图，没有则删除此行]，仅提取功能、字段、信息层级、必要分区与操作意图；将这些功能重新设计为图 A 的风格。对方游戏截图不提供美术方向，文案和数值只按本次已确认需求使用。

所有视觉统一为图 A 的清雅灵动道家 Q 版、精致高清手绘质感。深玉绿／青绿标题牌、主按钮与选中态，象牙米白细腻纸面内容区，温润暖金收边；圆润转角、轻巧层叠框体、细致云纹角花，少量太极、葫芦、玉珠和短流苏。玉面与纸面纹理克制，金饰柔和，边缘清楚、层级明确。保留白衣、白毛、纸面细节，清透但不过曝、不泛白，不做塑料反光或厚重金属框。

保留春节、元宵、中秋传统元素，在本页合适边角或背景少量融入[本次选用：朱红结绳／小灯笼／花灯与暖光／月轮玉兔／桂花与团圆纹样]。节庆是点缀，道家 Q 版是主风格，不必三个节日同屏，不做满屏红金，不遮挡信息或操作。

本页必须包含：[字段、模块、按钮、状态与已确认交互意图]。
本页布局：[根据功能确定的分区与主次]。沿用图 A 材质和控件语言，按本页功能重新排布。
人物／宠物／环境：[需要时列明已确认角色与场景；纯 UI 面板不额外加入大场景和全身人物]。保留圆润 Q 版比例及角色职业、发型、服装、法器差异；环境采用青绿仙山、云气、玉瓦道观和柔和景深。
文字与交付：[视觉稿使用已确认中文标签；接入素材按要求提供无字皮肤、独立文字层、透明区域和状态]。文字可读，装饰不侵入内容与点击区。
重切要求：[切片清单、原画布、九宫格边距、图集帧名坐标、锚点与透明规则；非切图任务删除此行]。先制作同风格完整母图或单件，再按清单重切和拼合验收，不直接沿用不符合风格的旧切片。
```

验收逐项检查：与图 A 画法、配色、材质和边框一致；已确认功能齐全；其他游戏截图仅影响功能结构；道家 Q 版、适量节庆、人物身份与曝光修正保留；文字、状态、透明边缘和切片拼合可用。仅换成绿金配色却沿用其他游戏皮肤或画法，不算符合规范。

## 3. 页面 01：登录

| 区域 | 内容与职责 | 文案来源 |
|---|---|---|
| 标题 | 正式游戏名“五行奇谈”，建立独立入口页的识别 | `product.title` |
| 主视觉 | 主城环境、新版 Q 道童与九尾狐，保留前景控件空间 | 对应视觉稿 |
| 当前服务器 | 当前选中服名、状态图标与状态文字 | `demoData.selectedServerId` 对应服务器，`serverStatusLabels` |
| 主要入口 | 选择服务器；进入游戏 | `pages.login.actions` |
| 辅助入口 | 切换账号、公告、设置 | `pages.login.secondaryActions` |

演示流程中，“选择服务器”前往 02；“进入游戏”在当前区服可进入时前往 03 选角。真实客户端应在自己的鉴权与区服可用性检查之后继续，本文不定义登录成功条件。

当前区服处于维护状态时，“进入游戏”进入 disabled 状态，显示 `messages.serverMaintenance`。区服名称和状态仍可读；“选择服务器”保持可用，使玩家能够改选。繁忙状态只是提示，是否排队由后续业务决定，演示不自动把繁忙区服禁用。

## 4. 页面 02：选择服务器

| 区域 | 内容与职责 |
|---|---|
| 页标题 | `pages.serverSelect.title` |
| 分类入口 | `pages.serverSelect.categories` 中四个分类，顺序保持 JSON 定义 |
| 搜索 | 使用 `pages.serverSelect.searchPlaceholder`；当前输入作为原生文本展示 |
| 区服列表 | 2 列 × 4 行演示卡片，按 `demoData.servers` 顺序排列；清单含八个区服 |
| 当前选择摘要 | `pages.serverSelect.selectionTemplate`，通过 `{serverName}` 填入选中服名 |
| 底部操作 | 返回；进入选角 |

演示初始选择由 `demoData.selectedServerId` 指定。每张卡片至少显示服名、状态图标与文字；推荐服附独立“推荐”徽记，选中服附勾选形状及“已选择”文字。分类的真实成员关系、最近登录记录和推荐策略由业务提供，本稿不推断八个演示服务器的生产分组。

选择可进入的区服时更新卡片 selected 状态和底部摘要；任一时刻最多有一个当前选择。点击“进入选角”前再次检查区服可用性，符合条件后进入 03；“返回”回到 01，并保留演示选择。

维护卡片使用 disabled 状态，显示维护图标、状态文案及禁入原因，不能成为可进入的目标。若已选服务器随后转为维护，保留选择摘要用于告知变化，禁用继续操作并提示改选。不能仅靠灰度或绿色/红色差异表达是否可进入。

### 搜索无结果

输入框保留查询值；卡片区域显示 `messages.noServerResults`，并提供 `pages.serverSelect.clearSearch`。无结果时禁用“进入选角”，即使此前存在选中 ID，也不能从不可见旧选择继续。清空查询后恢复列表，重新显示当前有效选择，再根据状态决定是否启用继续。

搜索匹配、拼音、区号和别名规则尚未确认；演示实现可先做服名文本匹配，并清楚标注该范围。无结果是空状态，不是网络故障提示。

## 5. 页面 03：选择角色

| 区域 | 内容与职责 | 数据来源 |
|---|---|---|
| 标题与当前服 | 选择角色、当前服名 | `pages.characterSelect.title` 与选中服务器 |
| 左侧槽位 | 两个已有角色、一个创建空位 | `demoData.characterSlots` |
| 中央主视觉 | 选中 Q 道童与同行九尾狐 | 选中角色和对应视觉素材 |
| 右侧信息 | 名称、职业、等级、所属区服、同行灵宠 | `pages.characterSelect.fields` 与选中角色数据 |
| 底部操作 | 返回选服、进入仙境 | `pages.characterSelect.actions` |

登录宠物由 `demoData.loginCompanionId` 引用，初始选中角色的 `companionId` 引用同一只九尾狐，右侧同行灵宠显示“九尾狐”。初始选中角色由 `demoData.selectedCharacterId` 指定。第一个槽位展示选中道童的姓名、28 级和职业；第二个展示未选的清铃、16 级与女性小头像。清铃的职业、详细形象及同行宠物未指定，JSON 使用 `null`，接入时省略未知字段，不补写推测内容。

点击已有角色槽更新唯一 selected 状态、中央展示和右侧信息。当前视觉稿完整呈现初始道童状态；另一角色的完整选中主视觉需要后续素材任务补齐，不把初始道童图当作清铃的形象。

空槽采用加号形状与“创建角色”文字，点击只进入待实现的创建角色流程入口；空槽不是可进入世界的角色。若当前焦点或选择位于空槽，禁用“进入仙境”，显示 `messages.chooseExistingCharacter`，同时保持创建入口可用。

“返回选服”回到 02；“进入仙境”只有在选中已有角色且区服可进入时可用。角色归属需与当前服匹配，区服切换后由业务刷新角色列表。本稿仅演示当前服的角色，不定义跨服角色或创建角色的持久化行为。

## 6. 页面 04：主城 HUD

[04_main_city_hud_2560x1080.png](04_main_city_hud_2560x1080.png) 以 [v4 主城人物预览](../qdao_chibi_game_pack_v4/preview-main-city_2560x1080.png) 为固定背景，用原生 SVG 和通用控件重做既有“战斗”“观战”“角色”三个按钮，文案从 `pages.mainCityHud.actions` 读取。没有重新生成或改画背景与人物。沿用统一玉绿、米白、暖金的控件体系，装饰与点击区域分离。

制作来源是 [hud/build.mjs](hud/build.mjs)，并非生图提示词。[HUD 说明](hud/README.md) 提供重建步骤，[placement.json](hud/placement.json) 记录实际位置。客户端优先使用 [无字透明皮肤](hud/hud_skin.png) 加原生文字；[完整透明 HUD](hud/hud_overlay.png) 已含标签，不能再重复叠字。

本轮只确定三个入口的名称与美术表达，不补写战斗匹配、观战列表、人物属性弹窗或网络协议。正常、选中与禁用的视觉规则沿用第 8 节；功能是否可用由后续客户端业务提供。点击这些入口的运行时效果尚未接入。

## 7. 场景 05：森林石桥战斗背景

[05_battle_scene_2560x1080.png](05_battle_scene_2560x1080.png) 重绘既有森林石桥战斗背景，沿用“五行奇谈”的青绿山水、圆润场景和暖金光线。此项交付为静态背景图片，保持与登录、选角和主城画面的整体风格一致。

本轮不定义战斗技能、血条、回合、敌我编队或操作 HUD，也不把背景视为可行走地图。角色动画、战斗系统、碰撞、导航、遮挡图层及客户端资源接线需要后续明确任务。

## 7A. 场景06：战斗入场与可叠加层

[战斗入场插画](06_battle_entry_loading_2560x1080.png)包含金发带Q道童、九尾狐灵玥与森林石桥目的地。场景05依然是无人战斗底图，两者用途不同。插画没有烘焙进度、加载提示或额外标题。

[真RGBA纯云气](06_battle_entry_clouds_fg_2560x1080.png)替换了带旧道童的云层；中央空区Alpha为0。暗色遮罩复用原黑色Alpha178图，代码可用黑色矩形opacity=178/255等效实现。建议渲染顺序为过场底图、可选运动云层、可选暗幕、客户端动态文本；持续时间、曲线和加载进度绑定未定义为已实现。旧路径和新路径对应见[过场清单](transition_manifest.json)。

## 8. 状态规范


状态 ID 使用稳定英文键；用户可见文字从 JSON 读取。下面定义交互要求；通用控件已有的状态资源、九宫格与文字边距以 [组件说明](components/README.md) 和 [组件清单](components/manifest.json) 为准，状态图片本身不实现事件。

| 状态 | 视觉与文字 | 操作规则 |
|---|---|---|
| `normal` | 清晰边界和完整标签，未显示选中勾记；保留足够辨识度 | 满足条件时可点击 |
| `selected` | 强化描边或选中框，同时显示勾记与“已选择”；分类页签也需要形状/文字辅助 | 保持唯一选择，重复点击不产生第二个选择 |
| `disabled` | 降低强调程度，同时展示禁用原因或状态图标；文字仍可辨认 | 不执行继续、选择或进入动作；适用的改选、返回、清空等恢复操作保持可用 |

服务器 selected 不等于推荐；两种状态使用不同标记。维护是服务器业务状态，会使进入操作 disabled。空槽是创建入口，不是维护状态。键盘焦点和指针悬停也不能冒充 selected；原生控件应提供可辨认的焦点边界，并采用与视觉顺序一致的导航顺序。

## 9. 接入时的完成标准

剩余任务与新窗口的执行顺序由 [后续交接文档](../docs/WUXING_QITAN_HANDOFF.md) 统一维护。以下项目供后续客户端任务验证，本次视觉稿不宣称已经完成：

- 各页面显示文案与 `copy.zh-CN.json` 一致，正式名称使用“五行奇谈”；动态文本和演示数据有独立数据入口。
- 登录与选角展示同一形象方向的灵秀 Q 版九尾狐，同行灵宠文案与图像一致。
- 主城保留“战斗”“观战”“角色”三个入口语义；森林石桥交付按静态战斗背景使用。
- 登录、选服、选角之间的继续与返回符合上述演示流程；未定义业务以明确入口保留。
- normal、selected、disabled 可辨认，区服推荐与选中能够区分，状态提示不只依赖颜色。
- 维护区服无法进入；区服状态变化后重新校验；搜索无结果不能误用隐藏的旧选择继续。
- 空槽进入创建入口而非游戏；缺少已选角色时不能进入仙境；切换已有角色会同步主视觉和信息。
- 原生文本没有与视觉稿烘焙文字叠加；实际分辨率、触控/键盘操作和资源拆分由客户端检验。

游戏名已经确认。后续实现前仍需确认账号体系、服务端区服状态、分类/搜索规则、角色槽数、创建流程及主城按钮对应的业务。未确认项保留演示范围，不据此声明 Unity/FairyGUI 接入或生产业务可用。
