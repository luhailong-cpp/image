# 新窗口交接：原版23名Q道家角色剩余动画与游戏接入

> 本文件接手后的新增状态见 [2026-09-18混合分辨率接续记录](CONTINUATION_MIXED_20260918.md)。本窗口仍未确认实际内置2.5，新增绘图暂停；已开展旧512/新1024兼容代码、旧素材保护和新Unity验证。以下正文保留原交接时刻的历史状态，最新验证结果以接续记录及其绑定证据为准。

本文件是2026-09-18最新交接。**全部任务没有完成。** 用户要复制到新窗口继续；旧交接正文保留为历史，当前状态与用户最新要求以本文件为准。

## 一、用户目标和最新边界

原目标：使用指定提交9adcf9291e4a867601868889a5965f3cd48630ba的原23名角色00–22，完成八方向，每方向16张真实行走帧，30ms/帧、480ms/周期，每方向独立idle，保留原肖像和原移动速度9，完成实际游戏接入与验证。

用户后续要求“拉近看也清楚：提高原生细节与动画帧分辨率”，随后明确：
1. 剩余使用GPT Image2.5。
2. “必须确认2.5，暂缓新增绘图。”
3. “剩下的2.5就行，原来的不要管了。”

执行边界：
- **已有素材保留，不因为模型或高清升级重做旧图，不重标旧图型号。**
- 剩余新图必须能确认2.5；此前内置绘图授权有效，但不得在明知/有证据仍为旧版时继续生成剩余。
- 当前暂停的是新增绘图，不是取消整体任务。新窗口先确认实际入口；不能仅凭官方公告、配置文件或提示词声称本次用了2.5。
- 仅授权内置绘图；没有授权单独计费API/CLI。不擅自购买服务、发送API请求或更换收费入口。
- 原角色身份、游戏接入、切图和实际验证已有授权，不需要重复问“要不要继续”。若型号仍不满足，明确指出该真实阻点。
- 没有后台自动重试、定时器或生图任务。此次交接后不从旧窗口继续新绘图。

## 二、必须先读的路径

工作目录E:/work。主仓库E:/work/image，正式客户端E:/work/mmorpg-client。

按顺序读取：
1. 本文件。
2. E:/work/image/AGENTS.md，以及正式/隔离工程中适用的AGENTS.md。
3. E:/work/image/qdao_original_roster_v14_hd/CONTINUATION_STATE_20260918.json：逐角色逐方向实际已有帧。
4. E:/work/image/qdao_original_roster_v13/inventory.json：唯一身份清单，含精确ID、4096原肖像路径、基线SHA。
5. E:/work/image/config/image-generation.json 与 E:/work/image/docs/IMAGE_MODEL_POLICY.md：共享目标配置；用户本次“必须确认2.5”优先于一般允许隐藏后端继续的规则。
6. E:/work/image/qdao_original_roster_v14_hd/CLIENT_INTEGRATION_HANDOFF.md。
7. E:/work/image/qdao_original_roster_v14_hd/HD_CONTRACT_PROPOSAL.md。
8. E:/work/image/qdao_original_roster_v14_hd/tools/README.md 与 tools/RUNTIME_REVIEW_FORMAT.md。
9. 涉及旧候选时读V13/tools/README.md、tools/PUBLISHING.md及对应04/05/06子交接；它们的进度数字可能过时，本文件与实际文件优先。

使用imagegen和generate2dsprite技能，先读本机SKILL.md：
- C:/Users/luyua/.codex/skills/.system/imagegen/SKILL.md
- C:/Users/luyua/.agents/skills/generate2dsprite/SKILL.md

## 三、交接时真实完成度

本次交接已重新只读核对：V13实际01..16帧文件、最新状态JSON、正式目录一致。不要把strip.png、原图、参考图或试切样本计作新增完成动作。

| ID/角色 | 已有真实行走/idle候选 | 正式接入 | 仍缺 |
|---|---|---|---|
| 00_reference_topright_boy 金发带Q道童 | 128+8，完整封存 | 已完成V13 | 不重做 |
| 01_ice_sword_girl 冰剑少女 | 128+8，完整封存 | 已完成V13 | 不重做 |
| 02_fire_talisman_boy 火符少年 | 128+8，完整封存 | 已完成V13 | 不重做 |
| 03_lotus_healer_girl 莲花医者 | 128+8，完整封存 | 已完成V13 | 不重做 |
| 04_mountain_guardian_boy 山岳守卫 | 104+8，未完整验收 | 未完成 | 24walk |
| 05_celestial_musician_girl 天音少女 | 52+8，未完整验收 | 未完成 | 76walk |
| 06_thunder_caster_boy 雷法少年 | 16+8，未完整验收 | 未完成 | 112walk |
| 07–22其余16名 | 新动作0 | 未完成 | 每名128walk+8idle |

合计：**4/23名正式接入，19名仍未完成接入；缺2260walk+128idle，共2388张动作。**
文件存在不等于未封存候选已视觉通过；数字没有假定未来全无返工。当前没有已确认2.5的新图，没有完整已审批HD角色。

详细缺帧：
- 04：N、NE、E、SE、S、W均01–16完整；SW、NW各已有01/05/09/13，各缺02/03/04/06/07/08/10/11/12/14/15/16。
- 05：N、E、S均01–16完整；NE已有01/05/09/13，缺上述12张；SE、SW、W、NW各缺01–16。
- 06：S已有01–16完整；N、NE、E、SE、SW、W、NW各缺01–16。
- 04/05/06均已有8方向idle。不要为了型号升级重新画它们。

07–22精确身份以inventory为准：
07_moon_shadow_assassin_girl、08_alchemy_prodigy_boy、09_bamboo_archer_girl、
10_crimson_spear_girl、11_jade_fist_flat_top_boy、12_iron_saber_flat_top_boy、
13_short_hair_wind_blade_girl、14_short_hair_snow_summoner_girl、
15_water_dragon_scholar_boy、16_golden_bell_dancer_girl、
17_ghost_script_calligrapher_boy、18_desert_sun_monk_girl、
19_spirit_beast_tamer_boy、20_star_formation_master_girl、
21_lidazui_hair_cook_boy、22_lidazui_hair_waiter_saber_boy。

character_move_8dir已经比对为原00的旧别名资源，不是第24名独立角色。保留旧4帧包；不能说旧4帧包本身已完成新接入。

## 四、Images2.5阻点与新窗口第一步

配置当前目标gpt-image-2.5-sunburst/quality=max、内置优先。官方公告：
https://openai.com/index/introducing-chatgpt-images-2-5/
官方API模型：
https://developers.openai.com/api/docs/models/gpt-image-2.5-sunburst

本窗口内置image_gen只暴露prompt、referenced_image_paths、num_last_images_to_include，没有model/quality参数。配置和提示词不能强制切换隐藏后端。

06新返回的S05/S09/S13三张原生1254图，PNG中本次c2pa.created软件信息都是{name:gpt-image,version:2.0}，时间为2026-09-18 09:41:40、09:42:49、09:43:33UTC。这是新调用创建记录，不是拿旧参考图版本推测。
- 证据：E:/work/image/qdao_original_roster_v14_hd/model-evidence/20260918-new-six-keyframes.json
- 提取器：E:/work/image/qdao_original_roster_v14_hd/tools/inspect_image_provenance.py
- 提取校验PNG CRC/SHA，不验证C2PA签名；实际隐藏API型号仍未披露。正确结论是这些结果**不能确认为2.5**，不要扩大成对所有官方入口的断言。
- 三张已保留，但没有导入候选、没有通过视觉审批、没有发布，不算2.5完成。

先核实新窗口实际可用入口/工具能力；若入口已变化且有合理新证据，可用一张必要剩余帧验证来源后再批量。若仍是相同旧入口且没有新证据，不要不断生成旧型号来试运气，也不要默默调用收费API。保留暂停并清楚报告阻点。

## 五、在制原图与拒稿位置

统一V13根：E:/work/image/qdao_original_roster_v13。

- 04旧候选：candidate/04_mountain_guardian_boy；旧剩余源映射见processing/frame-sources.json，原始批次在generation/04_mountain_guardian_boy。已有返回但未导入的SW批次不能自动算完成。
- 05旧候选：candidate/05_celestial_musician_girl。最新旧授权NE-cycle-fill-v3保存于generation/05_celestial_musician_girl/NE-cycle-fill-v3；4×4格过小并有伪影，已拒稿未导入。该目录STOP_STATUS_20260918.md有记录。05没有HD/2.5调用。
- 06旧候选：candidate/06_thunder_caster_boy。S-half-v2导入03/07/11/15；S-threequarter-v1导入04/08/12/16，故S现已16张。S-half-v1边缘问题、E-full-v1低格分辨率与步态问题、E-keys-v1关键腿不交替均未作为新合格动作导入；不要复活拒稿。E-keys-v2只有提示词，没有实际返回图片。
- 06身份参考：references/06_thunder_caster_boy/original-identity-1024.png。
- 06高清试图：generation-hd-pilot/06_thunder_caster_boy。S01-single-v1原生1254是早先目标2.0的样本；S05/S09/S13是前述不能确认为2.5的新探针。每个批次有*-raw.png、*-prompt.txt、*-tool-result.json。原始工具默认目录中的文件也保留。
- 04已有原生样本：generation-hd-pilot/04_mountain_guardian_boy/E01-native-v1。属于先前样本，不重标2.5。
- V14仅有04试切：E:/work/image/qdao_original_roster_v14_hd/candidate/04_mountain_guardian_boy/walk/E/01.png，1024输出；原生1254、人物高1145、factor0.685933、独立重建通过，但状态partial_sources_pending_visual，manifest/qc incomplete。它不是新增缺帧完成，也不是完整HD。
- 试切证据：该候选review/validation-E-01.json。
- 低清拒收安全验证：V14/model-evidence/readonly-native-minimum-guard.json；先前直接导入测试被自动审批拦住未执行，后以禁用全部写函数的内存探针验证627原生格在写入前被实际管线拒绝。此问题已用安全方式验证，不需重复提交同一被拦动作。

## 六、必须处理的分辨率兼容问题

用户要“原来的不动”，已有04–06候选是512输出，剩余新图又要原生更清楚。**当前客户端完整V14只支持全角色1024，失败时整角色回退同ID V13；尚不支持在同一个角色中按帧混用512与1024。**

这是真实未完成项，不能掩盖：
- 不能为了满足完整V14门禁重画旧04–06素材。
- 不能把旧512PNG扩画布/放大到1024并声明原生高清。
- 不能直接把不完整V14强行发布、关闭完整性检查或把旧512与新1024放在同一104PPU合同里。
- 新窗口需要在保留旧源图和外观的条件下确定兼容实现；若做按帧/资源分辨率的兼容加载，须显式记录来源尺寸、各自PPU、统一世界几何、缓存生命周期和完整库存，并新增真实测试。不要说当前已经支持。
- 07–22从零开始，确认2.5后可直接按完整原生高清合同制作。
- 不要擅自用新帧降到512的运行包冒充“拉近高清验收已完成”。最终清晰度以真实游戏近景为准。

V14已实现合同：原生完整cell两边>=1024；1024RGBA输出；runtime137PNG=128walk+8idle+portrait；review/strips中的8条16384×1024图不进Resources。root[512,942]，PPU104，pivot(.5,.08)，世界帧高等于旧512/52。16×30ms、speed9不变。
新原生大图实际工具常只返回1254，即使提示词写2048；按真实尺寸记录。4×4或2×2把1254分格不能满足原生1024要求；优先单人物单帧。不要以大画布文件名或插值放大冒充细节。
原肖像用指定4096原图全图降采样1024，不裁脸、不重画。

## 七、生成、加工、验收要求

- 每个新动作必须真实生图；禁止复制、镜像、扭曲、插值或平移同姿势来凑16帧。
- 保留精确prompt、原raw、真实tool output_hint/receipt、SHA和原生cell映射。历史记录保真。
- 方向顺序N/NE/E/SE/S/SW/W/NW。关键01/09相反领先腿；05/13相反抬脚；必须目检遮挡和靴子，文件SHA不同不证明正确步态。
- 固定角色common_scale：04/05 .84，00–03/06 .88。新角色根据实际原图定一次；不逐帧bbox fit。
- V13 alignment v2/root[256,471]，V14按2倍root[512,942]；不要混入旧吕洞宾v3合同。
- 标准chroma100/150；05紫裤必须purple-preserve50/75。透明源仅去alpha<=8噪声；原生触边要真实修图，不靠裁切/平移掩盖。
- 浏览器真实加载实际PNG，查16帧、30ms、480ms回环、独立idle，查看正常/放大、01/05/09/13和15/16/01接缝。
- 完整素材先独立源重建，再用与当前manifest/QC/图片SHA绑定的fresh-review-input和具体逐方向观察封存。改图会使旧审批过期。
- V14额外要求native_resolution_review、closeup_1080p_review及具体备注；只跑数值工具不能代替目视。
- 封存00–03不再跑pipeline import/review或不带require-visual的verify覆盖结果，不重做旧验证。

## 八、客户端和真实验证已完成内容

正式：E:/work/mmorpg-client。
唯一隔离验证：E:/work/tmp/qdao-original-live-candidate-20260917。
不要重建、整体覆盖或改用旧qdao-original-verify-20260917。

03已正式发布，证据：
E:/work/image/qdao_original_roster_v13/runtime-validation/accepted-originals-run3
真实Edit329/329、Play21/21；新增03的148个正式文件与实测输入一致，原2666角色文件未改。旧交接里“03仅stage，下一步run3”已经过时，不能重复执行。

当前代码最终验证：
E:/work/image/qdao_original_roster_v13/runtime-validation/hd-publication-gates-run1/final-result-review.json
SHA fbb8230ef662edc5d060ca9862cf6b5e2f88ea81895da128650ec23cf4d1e03b

结果：
- Edit349/349、Play35/35，failed/skipped0、exit0。
- 发布器15项门禁测试通过。
- 46源码/meta/相机配置绑定一致；25973测试输入0增删改。
- 实际Original4、HD0；12张正常视角PNG来源/尺寸通过，12个空近景记录被正确拒绝。
- 测试范围为真实离线天墉沙盒，不是在线登录验收；HD测试有内存夹具，不能算真实HD角色成品。
- 现在正式仅QdaoOriginalRosterV13/00–03，没有正式QdaoOriginalRosterV14目录。

已实现按方向加载17张纹理、最多当前+观察两方向、共享引用计数与战斗/残影生命周期；完整V14优先，同身份V13回退。索引只存元信息，不急加载23×137图。
HD完整真实素材尚未存在，因此尚未验收真实HD近景和全套动作。

每次新资产或源码变化必须建立新run，不能覆写/复用旧run当作新通过：
stage完整已审批角色 → 新Edit输入快照/绑定 → 实际Edit → 新Play输入快照/绑定 → 实际Play → 看normal/nearest真实截图 → post全输入比较 → publisher dry-run → execute → 独立保护旧资源审计。
工具入口：
- V13/tools/capture_unity_inputs.py
- V13/tools/run_unity_tests.ps1（显式传-Project上述唯一隔离工程；不要依赖旧默认）
- V14/tools/publish_original_roster_v14.py（先--help和读实现，不猜参数）
- V14/tools/RUNTIME_REVIEW_FORMAT.md：正式publish新增--runtime-visual-review，绑定本轮实际report/input/image SHA，不能填假passed。

相机当前脚底跟随，1080p默认ortho27，最近ortho5整帧约1063屏幕像素，顶部可能被裁；必须如实记录，不能偷偷改镜头来假通过近景。没有验证4K最近距离像素级清晰。

## 九、环境、并行与安全接续

- PowerShell。Python：C:/Users/luyua/AppData/Local/Programs/Python/Python312/python.exe，带-X utf8 -B，有PIL/numpy。
- Node：C:/Users/luyua/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe；Playwright可用msedge headless。
- Unity：C:/Program Files/Unity/Hub/Editor/6000.6.0f1/Editor/Unity.exe。
- 普通exec/view_image/本地referenced_image_paths在旧窗口出现Windows ACL helper错误；授权范围内require_escalated的exec成功。新窗口按实际环境尝试，不把环境错误当用户没授权。
- view_image若失败，曾通过elevated exec读取并显示完整参考图，再用最少num_last_images_to_include；不要把未经观察的任意文件当作图片已看过。
- image_gen返回图须从真实output_hint复制进项目，保留默认生成原件；不要输出巨大的base64文字日志。
- 图片仓库有其他窗口地图/UI工作，禁止git reset/clean或宽泛覆盖。先看当前git状态，不回退别人修改来匹配旧测试。
- 本任务所有生图/Unity/捕获/验证进程均已收尾；临时V13预览8874已关闭。旧PID/session不再可用。新窗口仍应检查当前工程是否被其他窗口占用。
- 原04/05/客户端子代理已结束，新窗口自行建立新的有边界分工，不依赖旧代理存活。

## 十、建议新窗口执行顺序

1. 读本文件与状态/身份/客户端文件，只读确认当前状态未被其他窗口改变；不重新审核00–03。
2. 先解决实际2.5入口确认。若仍不可确认，明确报告阻点并保留暂停，不继续旧型号生图。
3. 处理04–06“旧512保留+新高清剩余帧”的兼容方案；不能以全角色重画替代解决。07–22可单独按全HD推进。
4. 从04 SW/NW缺失动作开始，05/06或07可以按角色隔离并行；每个角色只写自己的候选和批次，公共管线/Unity验证由一个代理独占。
5. 完成真实绘图、加工、视觉审批后，逐个实际接入、真实测试、发布；不预先把整23名registry当作完成。
6. 持续写真实进度，全部23名已完成正式接入并实际验证后，才可以说“全部做完”。

当前没有需要用户重新上传的原图，所需源图均在本机。不要只给方案后停止，也不要为绕过型号阻点假称新图来自2.5。

