# 透明边缘修复预检

本文件记录修复方案与尚待实测的项目，不代表已修改或验收正式素材。

## 已确认的旧处理与问题原因

既有 `character_move_8dir/clean_key_edges.py` 使用 RGB 到纯洋红 `[255, 0, 255]` 的距离阈值 `155`，命中的可见像素直接置为 RGBA 全零。该阈值已用于现有 32 帧。脚本还检查 1254 × 1254 画布、透明范围和底部脚点 `bbox[3] == 1179`，并更新相关清单／质检记录。

当前审查已逐帧确认发梢仍有品红细边。该删除式色键只能处理接近纯洋红的像素，未覆盖与发色、抗锯齿等混合后的颜色；曝光处理也可能使 RGB 更远离纯洋红。后两项的相对贡献尚未对原始中间文件做像素追踪，不据此断言某一步单独造成了残色。

扩大阈值会继续删像素并改变 Alpha，可能侵蚀轮廓、红穗、暖金反光或正常淡紫狐毛，因此不把扩大色键范围作为本轮方案。

## 最小试修

复用已安装 `generate2dsprite` 的去色键混色／溢色处理原语，先做两个隔离样例，输出到 `docs/style-repair-20260910/staged/`，不覆盖正式资源。

| 样例 | 已实际看过的发梢坐标，左上右下 | 用途 |
|---|---|---|
| `character_move_8dir/east_frame_01.png` | `[455, 97, 535, 177]` | 右向发梢污染试修 |
| `character_move_8dir/south_frame_01.png` | `[696, 126, 776, 206]` | 正面发梢污染试修 |

1. 读入当前 PNG、记录 SHA 和原始 RGBA，检查已确认区域内候选污染像素的 Alpha 分布。候选阈值仅作定位，不直接判定颜色错误。
2. 确认已安装脚本实际提供的函数及参数，优先直接调用不重算 Alpha 的去混色／溢色原语；只对指定发梢区域的受污染 RGB 生效。不能以未经确认的函数名编写调用。
3. 原 Alpha 通道逐字节保留，区域外 RGBA 全部保持。Alpha 为 0 的隐藏 RGB 保持。只做确定性后处理，不重绘角色结构和服装。
4. 若原语会重算 Alpha、全图改变正常颜色，或只适合半透明像素而当前残边为不透明像素，停止该调用并记录限制；不将无变化或新损伤的输出视为修复。
5. 在浅纸色 `#f1eedf` 和深玉绿 `#172a25` 下，生成原像素与最近邻 3 倍放大的前后比较。检查品红线是否减少、是否转为灰绿／黑线、是否形成硬切边或损失发丝。
6. 样例通过后，再扩大到该帧实际发梢轮廓和其余 30 帧；每帧保留独立验证。修复整批前确定是否仍有其他透明边缘问题，不因一个发梢样例通过就认定整帧所有边缘完成。

## 宠物保护规则

三只幼宠的残色只在局部少量边缘出现，属于较低优先级。红穗、红冠、鼻子、暖橙虎毛、暖金配饰和青绿尾尖不做全图去色。

九尾狐的大面积淡紫冷白毛是正常设计色。仅考虑此前已查看到的尾毛内隙局部边界（`[777, 438, 857, 518]` 与 `[202, 568, 282, 648]`），另做保护范围和浅／深背景比较。人物发梢参数不得直接套到整只九尾狐。半透明毛丝与白毛体积要保留；不能以“去紫越多越好”作为成功标准。

## 当前核实边界

此前 `continuation/edges/review.json` 完成时，36 / 36 唯一源文件在审查前后 SHA 一致，源素材未被修改。**本次修复预检的最新 SHA 核对未能在本子任务执行通道完成**：两次只读提权命令因自动审批超时未执行，普通沙箱报 `apply deny-read ACLs`，Node 内核也未能启动。主任务已接手读取与代执行。本文件不把此前 36 / 36 的结果冒充本次最新计数。

此时尚未确认已安装处理原语的函数名、参数和 Alpha 行为，亦尚无本子任务产生的试修输出；实际调用和当前哈希变化数量以主任务后续实测记录为准。
