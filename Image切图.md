# 主城高清地图制作、切图与加载规范

本规范适用于所有主城及其日景、节庆外观。用户本轮要求单块4K、整图尺寸以最终拼接为准、全部最高画质；本项目将4K交付标准落实为 **4096×4096 像素/块，整图由最终行列数决定，不预设16K。** UI 控件的九宫格伸缩继续遵循原 UI 规范；本文的周边加载指地图分块流式加载。

## 1. 本轮状态与模型

本轮目标为 `gpt-image-2.5-sunburst-2026-09-08`，七套外观均使用最高质量 `max`。这是目标配置，不是已经生成或已经验收的记录。本轮新高清素材尚未生成，需在获得单独计费 API 路线授权后，使用能够显式指定该模型的入口执行。当前内置 `image_gen` 没有模型选择器，宿主官方说明仍为 GPT Image 2；提示词不能将其后端切换为 2.5，也不能将其输出记成已经验证的 2.5。

本轮预备包入口：[qdao_city_tiles_4k_20260916](qdao_city_tiles_4k_20260916/README.md)。其中布局参考、预备裁图及手写提示词不等于已经生成的高清重绘。

模型选择遵循 [项目模型策略](docs/IMAGE_MODEL_POLICY.md)。以后重制时重新核对最新正式型号、实际账户可用性、尺寸和质量参数；新一轮单独保留版本与来源记录，验收后替换。不得把文档规则更新写成素材已经升级，也不得默认自动使用单独计费接口。

尺寸和入口依据：[OpenAI 图像生成指南](https://developers.openai.com/api/docs/guides/image-generation)、[宿主图像生成说明](https://learn.chatgpt.com/docs/image-generation)。本轮核对的 GPT Image 2.5 单边输出上限为 3840 像素，因此不能请求单次原生 4096×4096；采用下述多张原生重绘拼合方案，完整记录实际返回尺寸。

## 2. 每块 4K 与原生重绘方案

正式交付图块固定为 **4096×4096**，采用 Unity 常用的 2 次幂方形纹理尺寸。每套外观可使用不同的最终行列数，但同一地点的日景与节庆版本必须使用相同布局、行列、世界边界与导航。行列数依据现有布局、镜头近景所需像素密度、设备内存和实测决定，不为追求某个整图“K 数”硬定数量。

- 完整地图像素宽度 = `columns × 4096`；高度 = `rows × 4096`。记录准确宽高，不以含糊的单个 K 数代替。
- 每个 4096 图块由 2×2 张 **2304×2304 原生重绘图**构成。每张含 2048×2048 有效核心，四边各 128 像素上下文；相邻原生图共享 256 像素重叠区。
- 全图使用连续的原生子块网格：横向 `columns × 2` 张，纵向 `rows × 2` 张。子块核心步长为 2048；同一套全图坐标生成所有参考裁区，不将每个 4K 图块当作互不相关的新构图。
- 2×2 原生图按重叠接缝拼合后，范围为 4352×4352；裁去外围各 128 像素，得到正式 4096×4096 图块。跨 4K 图块边界也必须使用同一套共享上下文和接缝选择结果，不能分别裁掉上下文后直接假定无缝。
- 可先完成全图统一接缝计算再按 4096 分割，也可用分区工作集节省内存；两种实现都必须保持跨图块边界的像素、坐标和接缝决策一致。
- 最终美术不得插值放大。参考原图可以放大、裁区、补边作为布局 guide，但这些预备图必须标记 `guideOnly`，不能进入正式美术输出。不得把预备裁图写成已重绘、把锐化或放大写成新增原生细节。
- 标注方式为“4096×4096，多张 2304×2304 原生重绘拼合；最终未上采样”，不得写成“单次原生 4K”。实际返回不足 2304 的生成图不得放大补齐；应拒收并重做或重新评审制作方案。

### 2.1 镜头与像素密度

当前客户端正交镜头默认size=27，最近可到size=5；世界地图300×300。以1080像素高屏幕的最近镜头为例，可见世界高度10单位，需要至少108像素/世界单位才达到1:1采样；整图对应至少32400像素高，约8块4096。2160像素高屏幕同一镜头需64800像素高，约16块4096。以上为密度下限测算，不是固定整图规格，也不代表所有地图已制作该数量；按实际目标屏幕、最近缩放、留白与硬件实测确定。源图像素充足仍须检查真实细节、过滤与导入质量。

方形原生分块的世界宽高比应保持1:1。现有300×300地图的完整方块网格行列数必须相等；以后非方形地图须使columns/rows与worldRect.width/worldRect.height一致，不能为凑数量非等比拉伸。

## 3. 布局、参考与接缝

保留现有主城的整体布局、道路宽度、桥梁位置、建筑足印、水岸、城门、出生与传送区域及前景锚点。重绘增加真实材质和结构细节，不移动现有阻挡边界。发生结构位移时须重新审查客户端/服务端导航与遮挡；验证完成前不得发布为兼容替换。

每张子块生成时应提供全城布局参考、该坐标的含上下文裁区、必要的已接受相邻图块；提示词标明参考用途。统一镜头角度、比例、光照方向、曝光、材质与色彩。日景和节庆只更换允许变化的装饰及光照，不改变可行走布局。天墉城沿用当前玉绿暖金版本；蓬莱、东海、揽仙保留各自灰蓝瓦、木色、石路、海岸自然配色，不强制统一青绿金。整体保持道家 Q 版与适量节庆元素。

接缝验收覆盖所有水平边界、垂直边界和四角交汇。道路纹理、屋檐、墙体、水岸及影子必须连续；统计色差与自动误差图只用于定位，不能替代 100% 像素审查与游戏内移动检查。机械拼接不得用布局 guide 像素填充成品。无缝难以解决的区域应重绘对应原生子块，保留返工原因与前后版本。

## 4. 当前七套外观与权威参考

以下为重制输入和现有版本，不是本轮 4K 成品。保留原图、原提示词和历史清单；本轮另建版本目录。

| `cityKey` / `variant` | 名称 | 当前参考原图 | 当前尺寸 |
|---|---|---|---|
| `tianyong/festival` | 天墉城·节庆 | [当前整图](tianyong_festival_hd_20260910/tianyong_city_master_6144.png) | 6144×6144，多张原生分区拼合 |
| `penglai/day` | 蓬莱岛·日景 | [原图](qdao_large_city_maps_20260912/penglai_island/penglai-island-native.png) | 1254×1254 |
| `penglai/festival` | 蓬莱岛·中秋月夜 | [原图](qdao_large_city_maps_20260912/festival_variants/penglai_mid_autumn/map-native.png) | 1254×1254 |
| `donghai/day` | 东海渔村·日景 | [原图](qdao_large_city_maps_20260912/donghai_fishing_village/donghai-fishing-village-native.png) | 1254×1254 |
| `donghai/festival` | 东海渔村·元宵灯会 | [原图](qdao_large_city_maps_20260912/festival_variants/donghai_lantern_festival/map-native.png) | 1254×1254 |
| `lanxian/day` | 揽仙镇·日景 | [原图](qdao_large_city_maps_20260912/lanxian_town/lanxian-town-native.png) | 1254×1254 |
| `lanxian/festival` | 揽仙镇·春节 | [原图](qdao_large_city_maps_20260912/festival_variants/lanxian_spring_festival/map-native.png) | 1254×1254 |

天墉城 6144 版本的布局来源是 [玉绿暖金原生稿](tianyong_festival_stylematch_20260910/tianyong-jade-gold-main-city-native.png)，1254×1254。天墉旧工具见 [build_hd_city.py](tianyong_festival_hd_20260910/build_hd_city.py) 和 [旧拼接说明](tianyong_festival_hd_20260910/README.md)，其 1024 核心、115 上下文参数仅代表旧批次，不能作为本规范的 4K 参数。当前七套的接入记录见 [场景交付](SCENE_DELIVERY.md) 和 [六图接入](qdao_large_city_maps_20260912/runtime/integration-delivery.json)。

## 5. 客户端 manifest 合同

每套已验证外观的正式清单路径：

`Assets/Resources/World/CityTiles4K/{cityKey}/{variant}/manifest.json`

对应 Unity Resources 入口：`World/CityTiles4K/{cityKey}/{variant}/manifest`。支持的组合即上表七项，不额外虚构天墉日景版本。

| 字段 | 合同 |
|---|---|
| `schemaVersion` | 整数 `1` |
| `tilePixels` | 整数 `4096` |
| `columns` | 实际列数，正整数；由本轮制作结果确定 |
| `rows` | 实际行数，正整数；由本轮制作结果确定 |
| `worldRect` | 对象 `{x, y, width, height}`；`x` 是 Unity 世界 X 最小值，`y` 是世界 Z 最小值 |
| `legacyForegroundCompatible` | 天墉城专用布尔值：现有前景逐件通过新图的几何、遮挡与近景清晰度验收后才允许为 `true`；缺少或为 `false` 时拒绝激活天墉新层，低清前景须重制后再验收 |
| `tiles` | 行优先的字符串数组，长度严格为 `columns × rows`；每项是无扩展名、无 `Assets/Resources/` 前缀的 Resources 路径 |

图块建议命名 `tiles/r01_c01.png` 起，行列号从 1 开始并保留足够前导零；清单中的资源路径示例为 `World/CityTiles4K/tianyong/festival/tiles/r01_c01`。实际文件顺序由清单决定，不依赖文件系统排序。

现有四地绘画范围维持 `worldRect = {x: 50, y: 0, width: 300, height: 300}`；世界导航范围与角色坐标不得随图像像素增加而放大。图像左上为原点，列向右、行向下；世界 X 向右，世界 Z 与图像行方向相反。以从 0 开始的行列 `r,c` 计算，每块世界宽高为 `width/columns`、`height/rows`，左边界 `x+c×width/columns`，上边界 Z 为 `y+height-r×height/rows`。

## 6. 视口与周边加载

加载集合必须包含**正交镜头视口投影到地面的完整可见范围，以及周边一圈图块**，再裁剪到地图有效行列。随镜头缩放、宽高比和窗口尺寸实时更新。3×3 是某些镜头位置与比例下的可能结果，不是固定上限；一个视口覆盖多块时不能只加载角色中心的九块而留下空白。

优先异步加载可见块，随后预取周边；根据移动方向提前准备相邻资源。设置卸载缓冲与可测内存预算，避免边缘来回移动反复加载；仅释放已离开保留范围且没有其他使用者引用的纹理。切换同地点日景/节庆时保持角色坐标、导航、场景及相机，先准备可见新图块，再原子切换，避免黑块或新旧外观长时间混用。

当前代码准备状态：可变网格、完整视口加一圈、异步限流、延迟释放及旧图回退已加入；同地点外观的可见新块全部备齐后原子换景仍须在正式发布前完成。当前未发布4K清单，不把此待办描述为已支持无闪烁换景。

## 7. 导出、来源和验收

七套外观均保留**原始无损 PNG** 和正式 **4096×4096 无损 PNG**；运行时也按 4K 标准加载，不能默降到 2048 或 1024。前景遮挡层另行保留 Alpha 与锚点。Unity 导入至少核对 `maxTextureSize=4096`（含各平台覆盖）、不做 NPOT 改尺寸、sRGB 和 Clamp。画质优先采用不损失细节的导入设置；filtering、mipmap、GPU 格式须通过目标平台实测决定，不能让默认最大尺寸或有损压缩抹平本轮细节。任何移动端降档或有损压缩均只能作为单独标识的备选方案，不替代本轮最高画质的 4K 标准。

4096² RGBA 解压后约 64 MiB/块，RGB24 约 48 MiB/块；实际 GPU 格式、mipmap 和平台导入结果决定真实占用。加载预算应按实测格式和最大视口覆盖范围计算，不按 PNG 文件大小估计显存。性能优化优先采用异步加载、按视口保留、及时释放和合理预取，不能以静默降低分辨率满足预算。验收清晰度同时测量切换耗时、峰值内存、移动卡顿及资源释放。

每批保存生产清单与 provenance，至少包含：版本 ID、时间、目标及实际返回模型标识、显式模型与质量参数、输出原生尺寸、请求/任务标识（不含密钥）、完整提示词、参考图用途和 SHA-256、全图和子块坐标、重绘原图 hash、接缝参数、每个 4K 图块来源集合、导出 hash、`finalArtUpscaled:false`、检验结论与客户端导入设置。API 实际未返回模型字段时明确注明缺项，不伪造后端证据。

发布前必须通过：

1. 全部预定图块齐全、尺寸正确、可解码、路径唯一，清单数量与实际行列相符；来源为本轮真实重绘，guide 无混入。
2. 分块重拼与最终美术像素一致，所有内部/外部子块接缝、4K 交界、四角交汇通过自动定位和 100% 视觉检查。
3. 主要地标、出生点、传送点、道路、水岸、建筑阻挡、角色前后遮挡及客户端/服务端导航对齐，无行走回拉或穿越。
4. 目标设备不同视口、最远/最近缩放、快速移动、地图边缘、日景/节庆切换及资源卸载通过真实客户端检查；记录截图和性能数据。
5. 生产清单、来源和回退版本可追溯；全部验证完成后，才把该外观的 `manifest.json` 发布到正式 Resources 路径。

准备目录和评审候选使用明确的未发布状态。验证期间可使用隔离测试副本，不能提前投放正式 manifest 让尚未验收的素材接管游戏。保留旧资源与旧清单用于回退；仅文档、工具或预备裁图完成不等于本轮高清图制作完成。
