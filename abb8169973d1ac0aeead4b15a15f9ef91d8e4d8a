# 天墉城节庆主城视觉稿：后续集成说明

当前交付为可审阅视觉稿，未替换生产地图、导航、前景遮挡或其他游戏代码。

- 新稿文件：`tianyong-festival-main-city-native.png`，实际原生尺寸为 1254 × 1254。尚未达到现有生产总图 6144 × 6144 的原生细节要求；普通插值放大不会补出真实细节。
- 生产地图使用 6 × 6 张 1024 × 1024 图块，覆盖 Unity 世界 X = 50..350、Z = 0..300。原画坐标原点在左上，向下为正；世界 Z 方向相反。
- 旧生产图的 150 × 150 步行掩码实测可走 5892 / 22500 格，即 **26.19%**。这是旧图诊断基线，**不是新稿的可走比例**；新稿尚未制作或测量导航掩码。
- 加宽道路后必须重建与新画面匹配的步行掩码，检查出生点 Unity (200, 0, 180)、主要道路、桥梁和地标入口的连通性；同步更新客户端与服务端导航，避免看得见道路却无法行走或位置回弹。
- 现有前景遮挡是针对旧图 r03_c03 灯檐逐行描出的轮廓，不能直接用于新稿。需按新建筑与灯柱重新制作遮挡及地面锚点，并验证角色前后排序。
- 确定高清总图后再分割生产图块，保留无压缩、Clamp 边缘采样等导入要求，检查图块接缝，并在游戏中实际验证行走、缩放与遮挡。

核对依据：`Assets/Scripts/World/Tianyong/TianyongPaintedCity.cs`、`TianyongPaintedForeground.cs`、`TianyongMapConfig.cs`、`Docs/TianyongMap.md` 与 `tools/tianyong_tile_pipeline.py`。
