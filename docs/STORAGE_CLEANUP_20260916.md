# 2026-09-16 素材清理记录

本次只删除已确认多余的处理图片和 Git 陈旧临时垃圾。

| 已清理内容 | 数量 | 释放空间 |
|---|---:|---:|
| V12 重复处理图片 | 88 张 | 280.08 MiB |
| Git 陈旧临时文件 | 10 个 | 12.97 GiB |
| 合计 | 98 个文件 | 13.25 GiB |

图片包括 82 张 `raw-sheet.png` 和 6 张 `sheet-transparent.png`。每张都保留了 SHA-256 完全一致的副本；当前构建、验收程序及页面没有直接使用被删路径。完整路径、大小、哈希和保留位置见[图片去重映射](STORAGE_DUPLICATES_20260916.json)。历史处理元数据可能仍记录原输出路径，可通过该映射定位同字节图片。已按具体路径添加忽略规则，避免重新提交这些重复输出。

保留正式动画、肖像、原图、参考图、`raw-sheet-clean.png`、对齐 v3 输入和变换记录、唯一历史图片及兼容旧路径。排除了本轮正在修改的素材和墨鸢游侠相关任务；没有按照“旧版”或“rejected”名称批量删除。Unity 安装下载缓存仍保留。

Git 清理对象仅为 Git 自身报告的 9 个 `tmp_obj_*` 和 1 个 `tmp_pack_*`，修改时间均超过 24 小时，删除前通过独占读取确认未被占用。检查结果见[Git 清理记录](STORAGE_CLEANUP_20260916.json)。正常对象包和提交历史保持原样；本地释放空间不代表 GitHub 上已有图片历史同步缩小。

校验已通过：清理前后 Git 对象连通性正常，Git 临时垃圾降至 0；4,888 个保留文件的 SHA-256 未变；88 张重复图的保留副本哈希全部匹配，被删文件全部消失。两个 JSON 清理记录均为 `completed`。
