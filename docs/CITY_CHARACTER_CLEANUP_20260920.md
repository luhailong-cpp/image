# 2026-09-20 主城与人物旧图清理

用户明确要求继续清理主城中不要的、非最终效果图，并将人物和原版人物纳入同一轮。范围限定在 `E:/work/image`。

本轮删除 **2,366 张 Git 已跟踪图片／动画，合计 1,092.98 MiB（约 1.07 GiB）**。本地文件与 Git 删除已处理；本任务未主动提交或推送。完整路径、删除前 SHA-256、Git blob、基准提交及保留依赖见[逐文件记录](CITY_CHARACTER_CLEANUP_20260920.json)。

| 范围 | 文件数 | 删除前大小 |
|---|---:|---:|
| 主城地图 | 82 | 481.94 MiB |
| 人物 V12 | 993 | 248.14 MiB |
| 原版人物 V13 | 1,291 | 362.89 MiB |

## 删除依据

- 主城：旧 4×4／32K 技术试作的过期效果图与 QA 截图、明确 rejected 的对比试拼，以及已被后续版本替代的裁切候选和旧预览。当前选择、最新制作及实际读取的输入被排除；同一版本系列额外保留最新两版。
- V12：八人自然版之前的导出帧和预览副本、何仙姑被素色自然版替代的旧导出。原始 source、提示词、当前候选、肖像身份来源和现行页面使用的资源均保留。
- 原版 V13：00／03 视觉修正前和05保紫重处理前历史快照中的派生 processing、walk、idle 与 review 图片。原始 source、历史 JSON、恢复前原角色包备份和冻结 baseline 保留。
- 历史记录中的图片路径可能指向本轮已删文件；它们记录当时结果，不伪造或重写这些验收记录。查看旧版本前可按 Git blob 或基准提交恢复。

## 当前继续使用的文件

- 主城当前19个坐标候选、19张清单图、源图、参考、提示词、接缝上下文和近期返修输入。整城仍是未完成状态，候选不能据此称为正式验收的最终图。
- 旧4×4的 `tianyong_plaza_4k_candidate_v3b.png` 虽非当前成品，仍被天墉后续布局裁图读取，因此保留。
- V12 当前八位角色：`candidate-stable-body` 中七位以及 `candidate-natural-body/29_he_xiangu`，其来源与验证数据全部保留。
- 吕洞宾对比页仍读取 `review/site/assets/pre-natural/24_lu_dongbin`，因此该目录保留；当前 `assets/v11`、`assets/v12` 和 sample 资源保留。
- 原版00–03当前接入素材，04–06混合版本所需旧帧、V14已完成与待续作素材，以及用户指定恢复的23人原版身份源包和基线。

## 验证与恢复

删除前核对18份当前人物 manifest，1,533个清单文件与记录SHA一致；删除后14,588个保留文件SHA-256与删除前一致，包括全部相关当前素材、来源元数据和原角色包。Git差异通过空白检查；没有执行重绘、素材发布、历史改写或GC。

所有删除项都由现有Git记录保存，逐项 `git_blob` 可恢复精确字节。删除旧效果图与清理工作目录不会自动缩小 `.git` 中的历史对象，也不会清除仍待上传的历史图片。旧导出目录需复现时，先恢复对应输入与图片或按原流程重建；当前正式素材入口以上述目录为准。

本轮临时扫描清单在报告保存后移除；忽略规则仅针对已淘汰的具体文件或已清空的旧派生目录中的对应图片类型，避免重跑旧脚本后再次跟踪这些过程图。
