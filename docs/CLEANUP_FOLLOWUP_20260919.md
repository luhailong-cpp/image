# 2026-09-19 再次清理 Python、JSON 与过程图

范围：`E:/work/image`。本次重新盘点全目录，并对同尺寸的图片、Python 和 JSON 进行 SHA-256 去重检查；不根据扩展名、版本号或文件名直接判断是否无用。

已删除 **25 个 Git 已跟踪文件，合计 7.87 MiB**；本地与 Git 删除均已处理，改动已暂存，本轮未提交或推送。逐文件路径、SHA-256、Git blob、保留副本和基准提交见[完整记录](CLEANUP_FOLLOWUP_20260919.json)。

| 类别 | 数量 | 判断依据 |
|---|---:|---|
| 一次性 Python | 7 | 已完成的 UI 字符串补丁、旧发布脚本备份及旧模型迁移助手；没有现行调用者。现行源码、构建器与发布工具保留。 |
| 中间 JSON | 3 | v8 已被成功结果替代的失败记录，以及重建命令会重新生成的渲染任务清单和报告。 |
| 曝光诊断／缩放过程 PNG | 7 | 仅用于已完成的重采样诊断和中间缩放；原图、backups、staged、完成报告和重建代码保留。 |
| 重复验缝 PNG | 3 | 天墉城 pair_r10_c07_c08 的 qa_v2 图片与保留的 qa_v3 同名图片字节完全一致；未删除地图拼接输入。 |
| 临时迁移文档副本 | 5 | .tmp 中的文件可从现有 Git 历史按 blob ID 恢复原字节；现行项目规则未变。 |

## 图片恢复与历史引用

旧 QA 或历史审计 JSON 可能仍记录被删过程图的原路径，它们描述当时的验证范围。重复验缝图可按本记录的 `keep_path` 与 SHA-256 复制回旧路径；其他 Git 已跟踪文件可从记录中的 `base_commit` 或各自 `git_blob` 恢复。未修改历史验收结果。

## 继续保留的内容

- 生图模型配置、素材 manifest、frame-sources、生成回执和正式验证记录。
- 原始图、正式成品、可重建素材所需的输入与备份，以及当前网页资源。
- 主城地图的多级 output：即使图片字节相同，也有后续拼接／修补脚本直接读取旧版本路径，不能直接删除。
- 当前人物、地图生产脚本和发布工具；本轮没有执行会重绘、发布或改写正式素材的脚本。

## 核验

删除前后共 **36 个保留文件 SHA-256 一致**，包括模型配置、项目规则、UI 页面、当前发布工具、v8 输入与正式状态，以及相关地图输出和保留的验缝图。v8 重建事务状态为 committed，不存在待恢复事务。没有改写 Git 历史；本次删除不减少历史对象的上传量。临时审计文件在报告保存后移除。
