# 2026-09-17 已追踪过程图清理

用户要求清理 Git 已追踪、不再需要的图片和过程图。本次删除并暂存 2,154 张 V12 过程输出，合计 829.42 MiB。不是仅清理 `.git` 垃圾，也不限于字节完全相同的重复文件。

## 删除范围与依据

- 旧处理器导出的 `walk-N.png` / `idle-N.png` 小帧、透明拼图 `sheet-transparent.png` 和测试 `animation.gif`：正式构建从保留的 `raw-sheet-clean.png` 和 `pipeline-meta.json` 重建帧，不读取这些导出副本。
- `raw-sheet.png`：仅删除与仍存在的原始输入逐像素一致的处理器副本；发现像素已不同的历史原图予以保留。
- `inspection.png`、大尺寸 `walk-review.png` / `idle-review.png`：不再被页面或保留审查记录使用的派生检查图；相应输入或完整角色导出仍保留。

逐项路径、字节数、删除前 SHA-256、Git 基准版本、保留依赖和排除原因见[机器可读清单](TRACKED_IMAGE_CLEANUP_20260917.json)。本次有 17 张候选因仍有引用、独有历史像素或缺少完整导出而保留；之前去重记录中的保留副本也保留。

## 保留与验证

正式 walk / idle / portrait、原始生成图、提示词、参考图、来源记录、清底母图、对齐 v3 输入、处理参数、备份及兼容路径保留。没有删除整个历史版本目录。正在制作的 V13 和主城地图不在清理范围。

清理前，8 位 V12 角色的媒体与数值检查通过。其中何仙姑的 QC 原本就是待视觉复审，其余 7 位通过已有视觉审批门槛；本次不修改任何视觉审批状态。清理后重新运行同一验收器，8 位角色的文件完整性与数值检查全部通过；712 个正式媒体、manifest、QC 和 411 个保留依赖均未变化。2,154 个删除项均已从工作区与 Git 索引移除；17 个保留例外均存在且不会被新规则忽略。完整结果见 JSON 的 `verification` 字段。

`.gitignore` 已按 V12 处理目录和确定的输出文件名添加规则，并给保留的特殊文件设置例外。生成过程仍可在本地产出这些检查图，之后不再自动纳入 Git。

## 恢复与 Git 空间

这些文件原本已提交，基准提交为 `71bc9b2bb04b7999613dbddab65afd121e9ee130`。必要时可按清单从此版本恢复单个文件：

```powershell
git restore --source=71bc9b2bb04b7999613dbddab65afd121e9ee130 --staged --worktree -- "清单中的具体图片路径"
```

删除已在当前 Git 暂存区记录，尚未由本任务 commit/push。删除后的新版本不包含这些过程图；旧提交历史仍保存原文件，因此不会自动缩小远端仓库历史体积。本次未执行历史重写或强推。
