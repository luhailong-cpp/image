# 2026-09-19 image 全目录清理

范围严格限定在 `E:/work/image`，扫描了 Git 已追踪文件、本地未追踪文件和被忽略的缓存；没有删除目录之外的文件。

本轮删除 1,633 个文件，释放 4.37 GiB。其中 923 张为 Git 已追踪图片，710 个为本地文件。此前两轮清理不重复计入。

| 已清理内容 | 文件数 | 删除前大小 |
|---|---:|---:|
| 本地 .work / Python 缓存 | 691 | 20.04 MiB |
| 旧场景可重新生成的复现副本 | 18 | 91.50 MiB |
| 已完成安装的本地 Unity 下载缓存 | 16 | 3680.09 MiB |
| 已替换、未进入现行帧来源清单的角色过程图 | 905 | 226.27 MiB |
| 内容已在 Git 中保存的旧交付 ZIP | 3 | 458.55 MiB |

[逐文件记录](IMAGE_DIRECTORY_CLEANUP_20260919.json)包含原路径、大小、哈希、类别、Git 基准版本及保留依赖。已追踪图片删除已暂存，尚未由本任务提交或推送。

## 判断依据

905 张角色过程图不在当前 `processing/frame-sources.json` 的逐帧阶段映射中；保留全部当前引用的切格、去底、归一化、清边与最终阶段输入，以及原图、提示词和正式输出。18 张 `scenes-sync/reproduced` 是复现命令的输出，生成输入和正式文件仍在。

Unity 下载任务记录为安装完成，并确认独立安装路径 `C:/Program Files/Unity/Hub/Editor/6000.6.0f1/Editor/Unity.exe` 存在；本次仅删除 `image/unity-download-resume` 的下载文件和临时任务文件，未修改软件安装。

3 个 ZIP 共 2,185 个文件成员，对应 1,027 个去重后的 Git blob，全部由现有 Git 提交引用。图片字节可完整从 Git 恢复；文本可从 Git 规范化的 LF 内容恢复，原 ZIP 压缩字节及混合换行布局不保证完全一致。成员路径、blob ID 和文本换行说明均保存在 JSON 的 `archives` 中。

## 保留与核验

正在制作的 64K 主城地图、V14 高清人物、正式图片、原始生成来源、当前参考和验收所需过程输入保留。旧 V7 处理图仍被两个历史检查页面引用，V8 备份与暂存图仍参与复现和验收，均未仅凭“过程图”名称删除。Git 推送正在运行，因此本次没有触碰 `.git` 临时文件、对象包或提交历史。

清理前发现 6 处旧提示词记录哈希与当前文件不一致，原文件与原记录均保留，未将其伪称为本次修复。清理前后按实际 SHA-256 比较，5,125 个保留文件全部未变化；1,633 个待删文件全部消失。本次扫描脚本和临时审计文件也已清除，长期记录只保留在 docs；结果见 JSON 的 `verification`。只对已确定的旧输出增加忽略规则，没有整体忽略仍被验收读取的 `processing`。

本地文件已实际删除；移除工作区图片不会自动缩小 Git 旧提交历史。
