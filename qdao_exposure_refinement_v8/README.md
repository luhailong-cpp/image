# 五行奇谈 · 全库曝光与泛白优化 v8

2026-09-09，已完成当前快照的正式图片修正并写回原路径。用户确认按分类精细调色处理全库，保留原画结构。

- [前后对照与可搜索全库清单](index.html)
- [交付报告](REPORT.md)
- [逐文件处理记录](processing.json)
- [最终验收](validation.json)
- [原始审计](inventory.json) · [同期新增与变更快照](supplementary_inventory.json)

本轮登记 2494 个视觉文件：初次2459，加35个新增属性切片/来源/预览。正式处理清单共1027个文件，953个修正，74个正常或技术素材保留；历史参考、诊断截图、备份和AI试样另列。检查期间更新的13个人物立绘以最新捕获版本为输入。

场景与整屏重点收敛天空、石台、水面和云雾的中高光；UI保留深色文字并柔化米白纸面；人物、白毛、白衣和物件仅轻收最亮反光。同套动作采用固定参数，天墉城40张母图/切片/预览经检查没有明显泛白，原样保留。

使用已获用户确认的本地选择性调色。在线性RGB中按亮度平滑降低曝光，额外控制近中性色高光；保持暗部、色相、alpha及全透明区域RGB。洋红来源键色、GIF透明色和帧结构、独立文字层与灰色禁用状态保留。复合UI由已处理素材按原排布重建，极薄抗锯齿边缘保留原色。没有为本次批量修正调用付费API，也没有把生成样张替换为全部正式素材。

验收：1027/1027文件通过；alpha变化、全透明RGB变化、异常增亮、新增压黑均为0。5个GIF共17帧、81组重复文件、36张地图切片及124帧图集关系均已检查。

图集限制：124帧图集与独立图标在本轮之前已有像素版本差异；本轮分别修正曝光且未增加不匹配像素，不宣称二者逐像素一致。逐帧基线记录在validation.json。本次没有借曝光任务改变已有图集造型和透明布局。原图中已经剪切成纯白的纹理不能靠调色恢复。

所有本次输入原字节以SHA-256存于backups/。staged/是通过验收的发布副本。处理从备份读取，重复运行不会再次压暗已处理图片。需要回退本轮时运行：

```powershell
python qdao_exposure_refinement_v8/tools/refine_library.py restore
```

回退会先核对当前哈希；其他任务后续改过的文件不会被静默覆盖。正常复核运行：

```powershell
python qdao_exposure_refinement_v8/tools/verify_exposure.py
python qdao_exposure_refinement_v8/tools/capture_latest.py
```

后续新图或再次重绘应先用capture_latest记录新原图，再经merge_latest、stage、复合重建、验收与publish流程处理；不要对现有修正版直接二次调色。源图及派生图均已覆盖，但旧生成脚本中另行绘制的文字/底色与历史哈希记录仍属于各原交付版本，不能把历史生成哈希当成当前文件哈希。

本轮只修改E:/work/image素材库及其中prepared准备副本，未写实际客户端、未提交或推送Git。检查期间的其他任务修改和文档保留。samples/里的AI登录精修稿是前一轮方向示例，它改变过部分绘画细节，因此未用作本轮正式替换。
## 2026-09-12 存储清理

`review/rebuilt/` 中35张字节完全重复的中间PNG已按[去重映射](../docs/STORAGE_DUPLICATES_20260912.json)清除。每张图的同字节副本、全部JSON、backups与staged均保留。旧重建报告中的这些图片路径是历史记录；查看时使用映射的 `keep_path`，恢复原路径时先核对SHA-256，再从该副本复制。当前前后对比页与正式／暂存验收输入不受影响。清理和后续托管建议见[存储说明](../docs/STORAGE_GUIDE.md)。
