# 07 月影少女：进行中的素材与离线验收

本窗口只负责 `07_moon_shadow_assassin_girl`。2026-09-23 本轮仍在持续制作与返修，本文是恢复工作用的进度记录，不是完成声明。

当前真实候选在 `07-tools/candidate/07_moon_shadow_assassin_girl/`，每张 PNG 的 `.generation.json` 给出真实选用 attempt、原生尺寸、SHA、导出操作、请求与回执。以实时文件为准；旧2026-09-21的0张快照已经过时。八向16 walk与8 idle已补成候选齐套，**尚未全部通过步态与比例验收**。

预览入口 `07-delivery-preview/index.html` 会随当前复核版本更新。每个完整方向生成深浅底16帧GIF，保存后重读时长为每帧30ms、每圈480ms。页面引用当前候选，因此旧revision中的清单SHA可能与后来替换的PNG不同，最后验收必须重新构建并绑定当时SHA。

## 尚在修正

- N：真实16帧与idle在位。01/04/09的头身或过步已重新生成；05/13从独立idle姿态画出向前落脚，正选用复核。仍需对整圈腿相位、头身、15→16→01→02做最终复查。
- NW：09–16已重画，修复旧后半圈窄身与错误同腿；05–08和11/15的腿序或转身/摆臂问题仍需继续处理。当前不是合格声明。
- NE/E：独立修复代理负责。NE头身06/07/08及13–15过渡已多次修订；E近远腿此前实际没有正确互换，正在由明确反侧抬膝稿逐步制作前摆→落脚，拒稿不选入。
- S：16帧已生成，并修02/06/13/15；代理正在完整离线复核，随后复核SE。
- W/SW：16帧均在位；代理继续修W过步、错腿与尺寸，SW的01/02/05/10和首尾体量仍在修。头带宽诊断可能被姿态/饰物影响，必须目视复核，不能盲按量测缩放后写通过。
- SE：16帧与idle早已在位。根此前发现06、15/16角度/比例变化，待再次全圈复核与必要重画。

## 来源与保留事实

只使用内置 `image_gen`，收费API调用0。配置目标2.5/max；入口仅返回图像和output_hint，没有披露实际model/quality，逐图保留null及host-managed未确认说明。不能把配置或提示词当作实际锁定型号。

早前按用户当时授权已清理1226个原图/回退/拒稿/中间与旧预览文件，精确清单 `07-tools/cleanup-20260923.json`，清理前69图源/导出验证与清理后最终SHA验证均已落盘。已删除的来源不能重建，不伪造为当前可复验原图；其原生尺寸/来源SHA来自清理前文本证据。

当前新增在制稿保留一份真实 `07-generation/<attempt>/raw.png` 以及精确prompt/request/result/原生尺寸SHA；尚在制作和验收期间不删除唯一在制稿。最终保留与清理由根根据用户最新明确指令统一处理，其他代理不要自行清理。

N09-v3曾出现 `image generation failed: connection failed: error sending request`，原始错误保存于该attempt。随后工具已恢复，N09-r20260923a成功，其他代理连续调用正常；这不是当前阻塞，也不能借此提前结束。

## 验证与交付边界

`07-tools/verify_candidates.py` 验证当前输出SHA/1024RGBA，保留raw的直接导出会在内存按记录重新生成并逐像素比较；历史已清理raw只验证真实文本清理记录，不能声明当前源像素通过。

`07-tools/root-visual-review-20260923.json` 记录根此前实际IAB检查N深浅底512循环、1024首尾和NW联系表的发现；后续替换后必须刷新验收证据。NE/E、W/SW、S/SE各代理各自写当前方向复核，最后由根统一对全8方向复验。

尚未接入Unity或正式客户端；未进行Git提交、推送、还原或Git清理；未修改其他角色。完成本角色后停止，不自动接下一角色。
