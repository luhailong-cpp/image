# 17 灵篆书生 N / NE / E 静态复核交接

本次范围为三个方向的 48 张行走帧和 3 张独立站立图。51 张候选全部齐全，静态复核完成；动态循环验收由主流程统一执行，本记录不宣称已接入客户端。

## 精确选稿

主流程使用 [selections.json](selections.json)，其 51 个槽位都指向实际导入结果，不使用旧库存数量推算。

|方向|独立站立|行走 01—16|
|---|---|---|
|N|idle-N-v2|默认 v1，08 / 10 / 14 使用 v2|
|NE|idle-NE-v1|默认 v1，06 使用 v2，09 / 13 使用 v3|
|E|idle-E-v1|默认 v1，05 使用 v2，09 使用 v3，10 使用 v4|

N08-v2、N10-v2 是从旧任务已完成生成事件找回的真实既成图，没有重复生成。回执里的原图字节与已保存源 PNG 完全相等，提示词逐字匹配 request。E10-v4 是本次唯一新增生成，用原身份、E 站立、E09 接触相位和已确认风格图实际附图，修正近侧后脚与远侧前脚的深度关系。

## 已做的检查

- 全部 51 张分别检查深底、浅底、正常 512 格和放大 1024 格；检查服装、道具持手、两只灵体、头身比例、鞋和发丝边缘。当前选稿未见裁断、显著透明残块或身份断裂。
- 每方向查看 01—16 的交替步态、01 / 09 对脚接触、05 / 13 支撑与摆腿交替；另外独立查看 15→16→01→02 深浅底接片。E13 放大复核为过腿阶段，未发现确定的高踢腿错误，因此保留 v1。
- `build_preview.selection_rows` 对完整 51 张运行成功：每帧原生单张 1254×1254，最终 1024×1024 RGBA PNG；51 个原图 SHA / 单帧来源唯一，最终像素无完全重复或水平镜像；透明边界不碰画布；所有脚底锚点为 `[512,942]`；全部沿用统一 0.88 缩放契约。
- [validated-selection-rows.json](validated-selection-rows.json) 保存逐图校验与来源信息；[static-qa-report.json](static-qa-report.json) 保存指标和预览 SHA。

|方向|行走高度范围|身体面积比例 CV|站立/行走高度偏差|
|---|---:|---:|---:|
|N|803—826 px|0.930%|0.185%|
|NE|783—825 px|0.663%|0.613%|
|E|802—860 px|2.571%|1.283%|

这些数值仅说明比例与锚点检查通过，不能替代运动连续性验收。

## 30 毫秒预览及待主流程动态复核项

全部 GIF 都重新读取验证为 16 帧、每帧 30 ms、每圈 480 ms、无限循环。

|方向|深底|浅底|
|---|---|---|
|N|[N-30ms-dark.gif](N-30ms-dark.gif)|[N-30ms-light.gif](N-30ms-light.gif)|
|NE|[NE-30ms-dark.gif](NE-30ms-dark.gif)|[NE-30ms-light.gif](NE-30ms-light.gif)|
|E|[E-30ms-dark.gif](E-30ms-dark.gif)|[E-30ms-light.gif](E-30ms-light.gif)|

动态重点：NE 02 / 04 与 12→13 的头顶高度差约 30 px；E 09→10-v4→11 的足部深度过渡，以及 12→13→14 过腿；所有方向的 15→16→01→02 首尾。静态没有将这些运动感受标记为“已经平滑”。主流程若观察到不自然跳动，应重新绘制对应单帧，不能挪动或变形已有姿势掩盖问题。

## 旧北向提示词证据校正

只读取了已知来源对应的单个旧任务日志 `rollout-2026-09-21T12-00-01-01a0c4b2-1ac3-7380-9bc1-dd41a5fbe468.jsonl`。找到全部 15 张旧 N 选稿的宿主 image_gen 完成事件，事件 result 与档案 raw.png 字节完全相同。

其中 14 张档案 request / prompt 比宿主返回的 `revisedPrompt` 多一个末尾 LF；N14-v2 完全一致。已在这些原图目录追加 `session-evidence-nne-final.json`，保留真实宿主元数据、精确 returned revisedPrompt、两种文本的 SHA 和事件序号，未改写旧档案或旧 SHA。历史调用采用内存提示词表达式，因此证据将“宿主返回的 revisedPrompt”与“直接捕获的已解析请求参数”明确区分，不能把前者伪称后者。

N08-v2 / N10-v2 使用自己的 `recovery-evidence.json` 和完成回执，两者 request / returned prompt 完全相等。E10-v4 则按本次新流程直接传入精确保存文本并归档。宿主未披露实际模型、实际质量，各图仍如实记录为未确认；配置目标不作为实际锁定证明。本子任务收费 API 调用为 0。

本子任务没有清理原图、拒稿或旧版本，没有覆盖其他方向，没有操作 Git，也未做客户端接入。
