# 05 天音少女 SW 静态选用复核

2026-09-23，当前选用索引见 `../SW-selection.json`。共16张真实生成单帧，全部原生1254×1254，透明导出1024×1024。04采用v2、09采用v2、10采用v4、11/12采用v2，其余v1。脚底锚点全部为(512,942)，处理参数固定0.84与purple-preserve50/75；没有镜像、复制、插值、扭曲或平移同姿势补帧。

已查看深浅底256正常接触表、原像素腿部放大表、512大小15→16→01→02接缝，以及新补帧的1024背景检查图。05抬屏幕右腿，13抬屏幕左腿；腿部相位交替、两脚分离、服装乐器与前左视角保持。出口没有发现明显洋红／蓝色残边，真实紫色飘带保留。

可见高度824–845像素；16→01在脚底固定时增加15像素。小幅脸部、发髻、衣纹差异仍存在，尤其10→11的左脚抬起与16→01接缝需要主代理全角色30毫秒连播复核。本记录是静态选用结论，不自动授予全角色循环或客户端通过。

`SW-16-30ms.gif`与WebP已生成；GIF逐帧元数据验证16×30ms=480ms，子代理未声称实时观看动画。主代理使用该选用集做八向离线播放验收。

SW10v2/v3脚相位错误拒收；SW09v1沿用交接拒收。SW04v1、10v1、11v1、12v1没有成功结果。请求、回执、原生尺寸和SHA均保存为文字记录。父代理核实最终游戏导出及引用后统一删除原图、拒稿和中间图，本次子任务不执行删除。
