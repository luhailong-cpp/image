# 05 天音少女静态补充验收：NW、既有 N/E/S、8 张 idle

检查日期：2026-09-23。角色仅为 `05_celestial_musician_girl`。

检查快照：`05-delivery-preview/revisions/complete-review-v1`；manifest SHA256 为 `b47adeff4c3889935c9475b5e527dea5e756ffa11b679cb2f0fb21ee779853f8`。本次只读检查 runtime，未修改图片、来源、选择表或其他角色。

**结论：本次检查范围的静态检查通过，无需因此重画的阻塞项。** NW 有可见的小幅身体起伏和较快的换脚阶段，记录如下。本文不能替代主任务的八方向浏览器动态检查，也不代表客户端接入通过。

## 实际查看范围

- NW、N、E、S 各 16 帧全身接触表，逐方向查看深底与浅底。
- 上述四方向的 15→16→01→02 首尾接触表，深底与浅底。
- 四方向全部腿部裁切，深底与浅底；NW 使用原生 1024 图的腿部 1:1 像素显示，相对于常规 512 世界画布为放大检查；旧 N/E/S 的原生 512 腿部使用最近邻 2 倍，仅用于检查。
- 8 张独立 idle 全身原生 512 合成，深底与浅底；另逐张查看头身 2 倍和脚部 2 倍检查图。

检查合成位于 `05-audit/final-neighbor-static`。这批合成只服务验收，允许最终清理；不得将这些放大合成称为新高清成品。

## 分项观察

| 范围 | 观察 | 静态结论 |
| --- | --- | --- |
| NW 01–16 | 01–06 可识别屏幕左脚支撑及右脚抬起，07–13 转为相反侧，14–16 返回下一循环的接触阶段。06→07 和 13→14 换脚较快，但两侧靴子形态及裤腿关联清楚，没有多腿、断腿、误镜像或整组同侧摆动。 | 通过；换脚节奏交由主任务实际 30ms 播放判断。 |
| NW 首尾 | 15、16 的屏幕左靴逐步接近下一接触，01→02 延续右靴离地；脸朝向、琴、头冠、衣裙和发带保持身份一致。 | 通过。 |
| N 01–16 | 左右腿前后与抬落状态有交替，双腿被裙摆遮挡的关系仍可读；15→16→01 保持同侧接触，02 进入另一侧动作阶段。 | 既有动作静态通过并按原像素保留。 |
| E 01–16 | 侧面重叠时远近腿分离程度变化，03/14 经过并足附近，04–08 与 11–13 有明确抬脚过程；15→16→01→02 的靴子方向一致，没有反向脚掌。 | 既有动作静态通过并按原像素保留。 |
| S 01–16 | 正面两半周期能辨认左右交替抬腿及落脚，靴面/鞋底随抬脚而变化；15→16→01→02 接触阶段连贯。 | 既有动作静态通过并按原像素保留。 |
| 8 张 idle | 八个朝向齐全；双脚为独立站姿，E/W 的远腿按侧视遮挡；头冠、发色、服装、绿结与紫金琴保持同一角色。正常与放大均未见缺脚或可见游离背景块。 | 静态通过。独立来源/像素互异另由来源审计确认。 |

## 比例、锚点与透明边界

独立读取最终像素，使用 alpha > 8 的包围框测量：N/E/S 的 48 张旧图底边均为 y=472（半开区间，最后可见行 471）；NW 的 16 张底边均为 y=943（最后可见行 942）。原图分别为 512 和 1024，不能直接混用像素单位。未用逐帧缩放、平移或扭曲修正姿态。

N 顶部 y=69–80、E 为 62–79、S 为 60–76；NW 为 149–173。NW11→12 顶部由 173 变为 149，差 24 个 1024 原生像素（常规 512 显示约 12 像素）；NW16→01 为 156→169，差 13（常规显示约 6.5 像素）。静态看是轻微步态起伏与衣发轮廓差异，没有身份/身高突然换成另一比例的严重问题；这两处须与主任务动态记录共同判断，本文不将“存在起伏”改写为“零抖动”。

深浅底实际查看中，未发现大块背景、明显锯齿硬边、裁断的主要肢体、白色矩形底或可见游离高亮碎点。旧 512 图放大时保留自身像素细节与半透明浅紫发带边缘，未假称已升级清晰度。边界判断区分衣物本身的紫色描线与游离杂点。

NW 极低 alpha 点已独立复测：

- NW01 `(706,832)` 与 `(804,810)` 均为 `[0,0,0,1]`。
- NW07 `(704,835)` 为 `[0,0,0,1]`，`(704,836)` 为 `[255,0,255,1]`；该 1×2 区域没有 alpha > 8 像素。
- 这些像素最大不透明度为 1/255，深浅底正常/放大检查均未形成可见碎点，属于非阻塞记录，不据此重画或侵蚀真实发带边缘。

逐帧文件 SHA 与测量记录见 `lineage/static/measurement.json`。来源审计的 128 walk + 8 idle、60 张保留 512 + 76 张 1024、16 个 GIF 每个 16×30ms=480ms，属于 `FINAL-SOURCE-AUDIT-20260923.md` 的独立文件检查结论；本次没有重新宣称客户端运行验收。

## 验收边界

本报告只负责所列方向和 idle 的静态观看及像素测量；NE/SE/SW/W 静态检查、八方向 30ms 实际浏览器播放、最终交付和清理由主任务汇总。未运行 Unity 或游戏客户端。
