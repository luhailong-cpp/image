# 15 水龙书生移动图交接（制作中，未验收完成）

本窗口仅负责 `15_water_dragon_scholar_boy`，不制作其他角色，不进行 Git 操作。实际库存以 `15-delivery-preview/runtime`、逐图 sidecar 和 `15-generation` 为准。本文仍为过程记录，完成后会更新结论。

目标为八方向各 16 张独立真实行走图及各 1 张独立站立图，交付 1024×1024 RGBA。所有新原稿均通过宿主内置 image_gen 逐帧生成；未调用收费 API，实际模型和质量未披露，记录为 host-managed/unverified。配置目标不代表实际返回值。

## 当前工作

- 原肖像：`../../q_daoist_character_pack_4096/15_water_dragon_scholar_boy_transparent_4096.png`（原身份文件仍在工作区，以上相对路径仅供说明，实际位置见 references.json）。身份缩略参考和已确认风格见 `15-reference`；实际请求逐一保存参考图路径。
- N/NE：各 16 张行走及独立站立已选；正在最终检查，N02 比例、NE06/08 相位已重绘，N06/07 重排，NE13 孤立背景灰点已清除并留记录。具体见 `15-review/N-NE-acceptance.md`（代理完成后写入）。
- E/SE：各 16 张行走及独立站立已选，但 E 换腿关系正在修正。SE06 已重绘低位迈步；SE 静态检查报告见 `15-review/E-SE-acceptance.md`。
- W/NW：W 已有 16 张候选，NW 正补帧与修复遮挡下的相反支撑。未通过美术验收。
- S：已有 16 张行走和独立站立；最新独立复核发现 S01/03 摆臂反复抬落、S05 抬脚过高、S07 鞋底重叠，四张 v2 已生成待比较和替换。S02/S04 使用 v4，S15 使用 v2，S16 使用 v3。
- SW：已有 01–14 的多版本真实原图，尚未最终选择。明确相反接触关键图为 SW09-v3（右下大鞋为前脚，左上小鞋为后脚，鞋尖朝左下）。05-v2 与13-v1为两次并腿过渡。06/08/10/11等曾换回错误脚，须按实际图核对，不能照帧号假定相位。SW08-v3、10-v3、11-v2、12-v2正在生成。SW15-v1 的真实工具事件为 failed，无原图，已保存 recovered-tool-event.json 和失败回执，不得计数；SW15/16仍需补齐。

## 文件与证据

- `15-generation/<批次>/request.json`、`prompt.txt`、`receipt.json`：真实提交与返回记录；原图原生尺寸通常1254×1254，以逐图记录为准。
- `15-tools/archive_frame.py`：真实原稿归档、透明清边、整格等比缩小和脚底注册；绝不制造姿态。`--scan`只做文件契约与来源检查，不代表美术通过。
- `15-delivery-preview/runtime`：被选中的候选交付图及来源 sidecar，仍待整套通过。
- `15-tools/build_preview.py --derived`：为完整方向生成深浅底16帧图、首尾图与30ms GIF；16×30ms=480ms。HTML也提供逐帧、背景和倍率控制。
- 浏览器 file:// 预览曾被工具安全策略明确拒绝，未绕过该限制。离线PNG逐帧和GIF结构检查可以继续，但不能把时长元数据检查写成已观察动态播放。客户端未接入、未测试。

## 续做规则

先核查已生成原图与真实回执；中断时工具可能已经产图但未复制。仅按本会话/对应子代理的 rollout 中精确 prompt 和 image_gen 完成事件恢复，不能伪造请求成功。修稿必须使用内置 image_gen。按用户最新继续指令，真实 raw 与文字证据保留，不做清理。所有拒稿、重试及中间图均不计入128+8。

完成前须更新本文为准确帧数、八方向链接、视觉检查结论、未验证项，并保存最终 source/selection/QA 清单。不因文件齐全就宣布全部通过。
