# 角色 14 NE 局部修订交接

2026-09-23，本代理仅负责 NE01、NE02、NE14、NE16。四槽真实图已导出到 `../14-delivery-preview/assets/walk/NE/`，原生均为 1254×1254 RGBA，成品均为 1024×1024 RGBA。来源与本次输出 SHA 见 `selected-finalize-ne.json`；后续全包清边/统一导出如改变成品 SHA，以成品旁最终记录为准。

| 槽位 | 选用来源 | 静态姿态复核 |
|---|---|---|
| NE01 | NE01-v3 | 左屏靴在前/较高，右屏靴在后/较低且露底，接续 16 的左脚前落地。 |
| NE02 | NE02-v4 | 左屏腿承重、右屏脚跟抬起，右靴随后在 03 离地；未交换脚侧。 |
| NE14 | NE14-v3 | 左屏弯膝前摆/靴抬起，右屏靴支撑；v3 去掉误画在靴后侧的大雪花装饰，恢复后缝。 |
| NE16 | NE16-v3 | 左屏腿向前伸展、靴较高，右屏腿后撑/靴较低；只有后侧右靴明显露底。 |

已看原图/导出图及整向 16 帧浅底、深底 256px 接触表，确认本次四槽与 03/04、13/15 的腿侧关系，以及 15→16→01→02 的前后脚顺序。01/16 到 02 的头顶高度仍约有 27px 变化，头部尺寸接近，需主代理在最终循环中判断承重变化与比例稳定性。残蓝紫边仍由主代理按用户授权做仅 RGB 轮廓清理。本记录是局部静态初验，不代替主代理的最终 30ms 循环、放大检查或客户端验收。

本代理未修改其他方向和共享状态，未运行 Git 操作。8 张生成均独立内置调用，没有复制/镜像/插值/变形凑帧；NE14-v3 是对该槽已有真实姿态的服装细节修订。实际模型、质量未披露，均记录 null，配置目标与实际参数分开；收费 API 调用 0。

## 拒稿与来源回执

- NE01-v2、NE02-v3：反腿，不选。
- NE16-v2：两靴露底过多，不选。
- NE14-v2：腿相位正确，但后靴出现前侧雪花饰，被 v3 替换。
- NE16-v1 是旧的只有请求、无原图/回执记录，不计生成完成。
- 上述首轮四图生成成功后，局部保存脚本因 JavaScript 不支持 `btoa` 而失败。未重画冒充原回执；从本代理宿主日志中的真实 Extension 完成事件，逐一核对精确提示词并恢复真实 savedPath、时间和 ID，分别保存 `host-generation-receipt.json`。对应 `tool-result.json` 明确标注 output_hint 为恢复说明，不冒充原始返回字符串。后续生成正常保存真实工具返回。
- 选用 01-v3、02-v4、14-v3、16-v3 均有正常 `request.json`、`prompt.txt`、`tool-result.json`、`raw.png.generation.json`。14-v3 的编辑来源 14-v2 具有上述恢复证据。

确认全包成品与文字证据完整后，可由主代理按最新 AGENTS 删除本工作目录的两张接触表、全部拒稿/原图与加工中间图。不要在最终导出完成前删除作为来源的唯一在制稿。
