# 08 炼丹童子 SW 素材检查

选用 **16 张独立行走 + 1 张独立站立**，清单见 [SW-selection.json](SW-selection.json)，逐图 SHA、实际库存与检查结果见 [SW-REVIEW.json](SW-REVIEW.json)。最终方向预览为 [sw-review-final-v1](../08-delivery-preview/revisions/sw-review-final-v1/index.html)。此方向快照只包含 SW；构建器显示其他方向缺失 119 槽属于此单方向快照的范围，不是全角色库存结论。

选用版本：03=v2、06=v4、07=v2、08=v2；其余行走及 idle 均 v1。共 22 张真实原图，选用 17 张、拒用 5 张；06-v1 仅有历史请求而无可见返回证据，标记中断，未认领任何未知缓存图。拒稿理由逐图保存在 `08-generation/<attempt>/review.json`。

所有选用图实际原生 1254×1254 RGBA，统一清除 alpha≤8 的低透明边缘，按整格 0.88 比例缩小并对齐脚底锚点 (512,942)，输出 1024×1024 RGBA。没有镜像、插值、变形或平移旧姿势生成新动作；统一输出对齐只作用于各自独立原图。17 个 raw SHA 与 17 个成品 SHA 均唯一。真实精确请求、prompt、receipt/output_hint、原图尺寸、SHA 和来源文字记录完整。

静态检查已完成：深浅底全身、1:1 脚部及 15→16→01→02 接缝。01 实际为远侧解剖 RIGHT 脚领先，旧 prompt 未改写；09 为近侧解剖 LEFT 脚领先，髋腿连接与前后遮挡真实交换。两手始终为近侧左手药瓶、远侧右手丹炉；双卷轴、草药、背包挂囊保持。03 初版因整体缩小重绘，06/07 同腿领先稿及08外踢稿已排除。最终静态图未发现明显透明残片、双腿断接、道具换手或接缝方向突变。

深浅底 GIF 各 16 帧，每帧 30ms，每圈 480ms；构建时逐帧验证时长。浏览器动态播放由主任务继续验收，当前未将浏览器/Unity/正式客户端验证标为通过。源图与过程图在主任务完成最终选用和引用核实前暂留，由主任务按项目保留规则统一清理。

本批仅调用内置 image_gen；收费 API 调用 0。目标来自既定批次配置 gpt-image-2.5-sunburst / max，实际提交 model/quality 均 null，实际返回版本/质量均未披露，记录 null / host-managed-unverified，未把目标当作实际模型证据。
