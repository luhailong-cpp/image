# 何仙姑原图候选组装复核

2026-09-14，lu_natural_east。阶段：原图组装已完成，未 process、未发布。

## 输入与原格范围

原始输入严格取自正式 `29_he_xiangu/manifest.json` 的 `sources`：s_e、n_w、ne_sw、nw_se 四张 paired sheet，和 idle。正式 manifest SHA-256：`91fa33a20c7223de4a40b1cf601c1e886ce4284aa62a5c694f5f6f4c0d71fd86`。

四张走路均为 1772×1772、4×4 网格；idle 为 1772×886、4×2 网格；所有格均 443×443。本次脚本只整格 crop/paste，不做任何 resize、bbox fit、角色平移、绘图、镜像或插值。

`provenance/original-manifest.json` 是正式清单的逐字节快照，保留全部递归原画来源；`provenance/manifest-inputs/` 保存上述五张原图的逐字节副本。清单中的 50 个去重上游文件路径/SHA 全部存在且吻合，记录于 `provenance/upstream-files-sha256.json`。新 E/W idle 的原始1254图、443整格、手写提示和来源/QC均另存于 `provenance/idle-fixes/E,W/`，原始生成文件仍留在 generated_images。

## 独立接触脚复核与换序

重新直接查看 manifest 所指原稿的 01/05 格，并在组装后再次查看全部八方向新01/05：

| 方向 | 原01接触脚 | 原05接触脚 | 新01–08对应原格 |
|---|---|---|---|
| N | 左：图左前脚较远，图右右脚露后脚底 | 右 | 05,06,07,08,01,02,03,04 |
| NE | 左：近侧右腿向左下后拖，远侧左鞋向右上在前 | 右 | 05,06,07,08,01,02,03,04 |
| E | 右：近侧右腿前、右手后 | 左 | 01,02,03,04,05,06,07,08 |
| SE | 右：近侧右大腿在前、右手后 | 左 | 01,02,03,04,05,06,07,08 |
| S | 左：正面图右脚在前、图左右手前摆 | 右 | 05,06,07,08,01,02,03,04 |
| SW | 左：近侧左大腿在前向左下、左手后 | 右 | 05,06,07,08,01,02,03,04 |
| W | 右：远侧右腿前、近侧左手前 | 左 | 01,02,03,04,05,06,07,08 |
| NW | 右：远侧右脚向左上在前、近侧左脚向右下在后 | 左 | 01,02,03,04,05,06,07,08 |

因此只将 N/NE/S/SW 的完整八帧循环轮转四格；没有单独互换接触帧。全部新01统一为解剖右脚前接触，新05为左脚前接触。逐格方向、源相位、源文件/SHA、源格坐标、输出格坐标、RGBA像素SHA见 `cell-source-map.json` 及每张 PNG 的 `.assembly.json`。`phase-plan.json` 保存相位表和判断依据。

## 独立 idle 变更

只替换 idle 的 E/W 两格。其他六格与正式 idle 原格逐像素相同。

- E：`review/he_xiangu_idle_fixes/E/idle-cell-443.png`，SHA `2e0603a1af1704e9321fd13c24cb95252112156f9de9bb40c462f4d92d3d1ab2`。
- W：`review/he_xiangu_idle_fixes/W/idle-cell-443.png`，SHA `da8b6c8d53790aaf852345022c814071f26decd54935cd0078ddf68129181116`。

两张均先由内置 image_gen 从同方向 walk02 原格编辑为独立站立，再整体1254→443归一化；本次组装直接使用既有443文件，没有再缩放。其头宽复核为 E225/参考224、W236/参考236，双脚放平、手自然下垂，无新增光效，衣服/莲花身份保留。

## 验证与导出接口

64个走路格、8个idle格均重新核对输出RGBA与所选来源格字节完全相同；72项通过。未轮转的 `nw_se.png` 连PNG文件字节也与原稿一致。所有其他输出也在保存后重开，与内存组装像素吻合。

下一步 root 可使用 `s_e.png`、`n_w.png`、`ne_sw.png`、`nw_se.png`、`idle.png` 进行共同 scale `1.037037037037037` 的 v3 导出。`candidate-sources.json` 列出五张候选文件及SHA、网格、方向顺序。

`assemble_he_xiangu_raw.py` 可复现所有来源保存、整格映射和验证。脚本只写本候选source目录；输入清单及每个来源SHA不符会立即失败。当前没有处理后的walk/idle、没有发布记录、没有改正式资源或代码。

本次统一的是接触脚相位与两张站立头型。原画既有高抬膝、后踢等中间姿势仍完整保留；不将整格轮转说成新的自然步态或所有中间帧均已重画。后续总验仍应参阅 `review/remaining-immortals-audit.md` 中的局部步态限制。
