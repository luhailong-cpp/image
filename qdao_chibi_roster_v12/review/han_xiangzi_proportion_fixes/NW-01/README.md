# 韩湘子 NW01 比例修正候选

原画局部头部比例修正已通过原格视觉检查，等待总流程组装和全循环验收。

使用 `final-raw.png`（内置 ImageGen 原生1254方图）或 `final-cell.png`（完整方图等比归一443方格，无人物 bbox 缩放）。来源链、所有尝试与取舍在 `final-provenance.json`，原始格来源在 `reference-provenance.json`。

发宽筛选指标：原 156.0 → 修正 173.0，正常 idle 170.0。只作筛选；已另看头发体积、脸型、头颈连接、手臂和两脚相位。

下身轮廓最佳整帧平移诊断：[0, 0]，对齐后IoU 0.9885。未通过脚、腿或 bbox 缩放修正。

`qc-processing` 与 `qc-despill4*` 仅供独立透明图检查。检查器关闭 trim，scale=1，无触边/裁切；现有4px去残色未改变透明度。不要直接导入这些居中QA输出。

未修改正式30、其他角色、处理器代码或客户端。
