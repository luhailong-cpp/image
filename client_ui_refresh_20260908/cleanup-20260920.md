# 2026-09-20 客户端准备目录精简

本轮按用户要求保留原图、已有脚本和清单，只删除旧截图与已经实际验证可恢复的准备副本，并提交本次改动。

| 内容 | 数量 | 删除前体积 |
|---|---:|---:|
| 可从保留文件逐字节恢复的 prepared PNG | 222 | 80.67 MiB |
| 旧 QA 截图／检查图 | 14 | 48.66 MiB |
| 合计 | 236 | 129.33 MiB |

`additional/` 中登录背景原图、参考、标题、提示词和制作记录保留；所有已有 Python、C#、PowerShell 脚本及 JSON 清单保持原字节。17张尚未确认精确恢复来源的人物准备图保留。原清单仍记录当时发布状态，不把旧报告改写成新客户端验收。

## 恢复准备副本

先核对来源（默认只读）：

```powershell
python -X utf8 -B client_ui_refresh_20260908/restore_prepared.py
```

恢复本轮删除的222张准备图：

```powershell
python -X utf8 -B client_ui_refresh_20260908/restore_prepared.py --restore
```

恢复工具只读取[清理清单](cleanup-20260920.json)列出的保留文件，逐项验证SHA-256后复制；只补缺失文件，遇到不同内容的既存文件会拒绝覆盖。来源均已在Git中保存。工具不执行旧发布流程，不修改客户端。

运行依赖prepared文件的原同步、重建或验收脚本前，先用上述入口恢复；本轮只保证保留副本的精确还原，不把旧脚本从已经更新的母图重新导出的结果当成同一历史版本。原图、脚本和原清单均未被更改。

14张旧截图属于历史实拍/检查记录，不能重新生成后冒充当时证据。需要查看旧截图或运行依赖截图的历史审计时，按清单的base_commit和path从Git恢复；恢复前后可核对记录中的sha256。截图不属于游戏运行资源。

## exact_qdao_slices 检查结果

该目录完整保留：104个文件约3.80 MiB，其中50张PNG。它仍是v10兼容UI切片和导入准备流程的源路径，`client_ui_refresh_20260908/sync_assets.py`及节庆准备流程会读取；不按临时副本删除。SVG、构建脚本、清单与正式切片都未修改。

## 验证

- 在独立临时目录实际恢复222张PNG，SHA-256全部一致；再次执行恢复没有重复写入。另验证了遇到被修改的既存目标时拒绝覆盖，且失败前不会补写其他缺失文件。
- 358个保留文件逐项SHA-256未变，包括原图、已有脚本/清单、恢复来源和exact切片。
- 对应客户端239张PNG及239份meta，共478个文件SHA-256未变；没有运行客户端同步或修改客户端文件。
- 全部236个删除文件删除前均与Git已跟踪字节一致；逐文件记录大小、SHA-256、Git blob及恢复位置。
- 本轮临时恢复图片和扫描文件在验证后移除；忽略规则只覆盖本轮准确删除路径。
