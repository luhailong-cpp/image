const path=require('node:path');
const fs=require('node:fs');
const {chromium}=require(process.env.PLAYWRIGHT_MODULE || path.join(process.env.USERPROFILE,'.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'));
(async()=>{const browser=await chromium.launch({headless:true,channel:'chrome'});const page=await browser.newPage({viewport:{width:1600,height:675},deviceScaleFactor:1.6});const errors=[];page.on('pageerror',e=>errors.push(e.message));const base=process.env.PREVIEW_URL || 'http://127.0.0.1:4345/attribute-panels/v10-preview/';for(const mode of ['hero','pet']){await page.goto(base+'?panel='+mode,{waitUntil:'networkidle'});await page.screenshot({path:path.join(__dirname,'review-'+mode+'.png')});console.log(mode,await page.locator('.panel-body > *').evaluateAll(els=>els.map(e=>({class:e.className,client:e.clientHeight,scroll:e.scrollHeight}))))}await page.setViewportSize({width:390,height:844});await page.screenshot({path:path.join(__dirname,'review-mobile-pet.png'),fullPage:true});console.log('mobile',await page.evaluate(()=>({width:innerWidth,scroll:document.documentElement.scrollWidth})));console.log('errors',errors);await browser.close()})().catch(e=>{console.error(e);process.exit(1)});
