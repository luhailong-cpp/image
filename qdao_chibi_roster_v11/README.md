# 五行奇谈 · 八位 Q 版人物

状态：整包收尾验收中。已完成角色以各目录 qc.json 和根 validation.json 为准。

用户要求的年轻Q版吕洞宾替代旧老头，并加入何仙姑、韩湘子。正式名单为灯穗小使、吕洞宾、狮鼓护卫、桂香药婆、墨鸢游侠、月兔机关师、何仙姑、韩湘子。

## 正式图片合同

每人独立立绘1024×1024 RGBA；八方向S、SW、W、NW、N、NE、E、SE，每方向四帧512×512 RGBA。脚点以左上为原点约(256,471)，横向半像素取整误差不超过0.5px。每帧120ms，四帧完整循环480ms。每方向另有2048×512横条与四帧GIF，两张2048×2048方向表。

全部8人预计408个正式媒体，其中8张立绘、256张独立移动帧、64张横条、64个GIF、16张方向表。GIF是预览用色彩量化版本；PNG是真RGBA正式资源。

## 制作与来源

原画使用宿主内置image_gen，每个人物、每个方向分别保留实际提示词和来源。处理仅作透明清理、完整主体裁切、同方向四帧统一等比缩放、脚线平移与图集排版；不把复制静态图、镜像或旋转当作新动作。

`512`、`1024`、`2048`为交付像素尺寸，原生生成尺寸与重采样分开记录在各角色manifest/processing/source来源链中。工具未开放model/quality参数，不能声称强制设置了high。

## 验收与接入

每个角色需通过数值与视觉验收，根verify_delivery.py再独立核对全部RGBA画布与透明度、脚线、唯一帧、GIF帧数和120ms时长、横条/图集与单帧逐像素一致性。27号采用另一风格修复任务已发布并按SHA锁定的新版本，不运行其旧装配脚本覆盖。

本次完成图片资产。现有客户端使用单角色硬编码资源以及八帧循环；应按本包每方向四帧manifest接入，不能直接将四帧横条覆盖旧八帧资源。本次不声称已完成客户端接线或游戏内验证。

旧24_crane_hermit已按用户要求撤下，仅保留历史记录，不进入正式总览和下载包。
