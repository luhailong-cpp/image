# 主城地图切图规范

本文件只规定主城地图（含日景、节庆外观）的高清分块、拼接、裁切和美术交付规则。适用对象是主城地图图块；UI、人物、图标使用各自规范。

## 1. 画质、尺寸与美术方向

- 每套主城外观按 **65536×65536 像素（整城 64K）** 制作，划分为 **16 行×16 列，共 256 个正式图块**。每块有效范围为 **4096×4096 像素（单块 4K）**，保留无损 PNG；外围拼接上下文不计入有效尺寸。
- 64K 是整张地图的实际像素与细节密度目标，不是把旧图放大后的画布尺寸。每块必须在游戏最近镜头下具有清楚、真实的结构和细节。
- 先逐区域生成或重绘高清细节，再拼接、切图。旧低清图可以作为布局参考，不能只放大、锐化或裁成小块后当作高清成品。
- 单次原生素材不足以覆盖正式图块时，可用多张原生细节图拼成一个 4K 图块；保留真实来源尺寸与拼合方式，不把拼合图标成单次原生输出。
- 美术统一采用更 Q 版的道家幻想风格：形体圆润饱满、比例可爱，树叶团簇饱满，画面明亮干净；沿用各地域的自然配色和既有主题。
- 高清主要体现在清楚的轮廓、结构、雕刻层次、石缝、瓦片及叶片组织。石材保持干净，减少写实裂纹、颗粒、脏污和密集微纹理；不能靠增加噪点或过度锐化营造清晰感，也不能以 Q 版为由留下软糊边缘或涂抹感。

## 2. 布局与拼接

- 沿用已确认的主城布局、镜头角度、建筑比例、道路、台阶、桥梁、水岸与出入口位置。增加画面细节和强化 Q 版造型时保持这些结构连续。
- 同一地点的日景与节庆版使用一致的 16×16 分块布局和坐标；各地点保留自身已确认的美术风格、配色及光照，并遵循上述 Q 版方向。
- 在统一的 65536×65536 全图坐标下规划所有分区，生成时携带相邻区域的重叠上下文。每块都属于同一张连续地图。
- 拼接先处理重叠区域，再裁出正式图块。相邻 4K 块之间也要统一接缝，不能独立裁掉上下文后就假定无缝。
- 道路、屋檐、墙体、水岸、树冠、阴影和地面纹理必须连续。几何错位应重绘修正；羽化只用于细小纹理过渡，不能用模糊掩盖错位。
- 图像与地图范围保持等比，不能为凑行列数量而拉伸变形。

## 3. 裁切与命名

- 图像左上角为原点，列向右、行向下；文件行列编号从 1 开始，按先行后列排序。每套共 256 块，编号范围为 `r01_c01` 至 `r16_c16`。
- 每套外观使用独立目录，建议 `{主城}/{外观}/tiles/r01_c01.png`、`r01_c02.png`，依次编号。
- 第 r 行、第 c 列图块的整图左上坐标为 `x=(c-1)×4096`、`y=(r-1)×4096`；正式图块各保留 4096×4096 的有效范围。用于衔接的外围上下文单独保留，重叠像素不得重复计入整图尺寸。
- 裁切清单记录主城、外观、版本、总行列数、整图像素宽高、文件名及每块在整图中的像素坐标和有效范围。
- 原生来源、完整提示词、参考图、重叠参数、拼接记录和文件校验值随批次保存。正式图块与参考裁图、过程稿分目录存放。
- 保存可重拼的清单与总览预览；总览可以缩小查看，正式图块保持无损 4K。

## 4. 切图验收

1. 每套 256 块齐全，编号、坐标与 16×16 清单一致；每块均可解码且为 4096×4096。
2. 按清单重拼后为 65536×65536，布局正确，没有缺块、重复块、错序或非等比拉伸。
3. 以 100% 像素检查所有横向、纵向接缝和四块交汇处，没有断路、错位、色带、亮暗跳变或模糊接缝。
4. 重点检查地砖、瓦片、栏杆、植被和建筑轮廓，确保真实结构清楚、Q 版形体圆润饱满、画面明亮干净；没有放大糊感、锐化白边、噪点或过量写实裂纹颗粒。
5. 在游戏最近镜头和跨块移动时核对清晰度与边界连续性。与地图相接的独立前景也应保持一致的画质、位置和轮廓。
6. 通过验收后交付正式图块、裁切清单、总览与来源记录；保留上一版和返工记录。

客户端分块加载、导入设置与接入验证另见[客户端说明](../mmorpg-client/Docs/CityTiles4K.md)；具体制作记录另见[制作记录](qdao_city_tiles_4k_20260916/README.md)。