# 主城地图切图规范

本文件只规定主城地图（含日景、节庆外观）的高清分块、拼接、裁切和美术交付规则。适用对象是主城地图图块；UI、人物、图标使用各自规范。

## 1. 画质与尺寸

- 所有主城按最高画质制作，每个正式图块为 **4096×4096 像素**，保留无损 PNG。
- **整图尺寸由最终分块数量与布局决定，不预设16K，也不沿用固定的旧网格数量。** 完整方格地图的宽度为 `列数×4096`，高度为 `行数×4096`，交付时记录实际像素宽高。
- 分块数量按地图内容、游戏内显示比例和最近镜头所需细节确定。每块有4K画布，还必须在游戏近景下具有清楚的真实细节。
- 先逐区域生成或重绘高清细节，再拼接、切图。旧低清图可以作为布局参考，不能只放大、锐化或裁成小块后当作高清成品。
- 单次生成能力不足时，可用多张原生细节图拼成一个4K图块；记录真实来源尺寸与拼合方式，不把拼合图标成单次原生输出。具体模型和本批次子块参数另记在制作记录中。

## 2. 布局与拼接

- 沿用已确认的主城布局、镜头角度、建筑比例、道路、台阶、桥梁、水岸与出入口位置。增加画面细节时保持这些结构连续。
- 同一地点的日景与节庆版使用一致的分块布局和坐标；各地点保留自身已确认的美术风格、配色及光照。
- 在统一的全图坐标下规划所有分区，生成时携带相邻区域的重叠上下文。每块都属于同一张连续地图。
- 拼接先处理重叠区域，再裁出正式图块。相邻4K块之间也要统一接缝，不能独立裁掉上下文后就假定无缝。
- 道路、屋檐、墙体、水岸、树冠、阴影和地面纹理必须连续。几何错位应重绘修正；羽化只用于细小纹理过渡，不能用模糊掩盖错位。
- 图像与地图范围保持等比，不能为凑行列数量而拉伸变形。

## 3. 裁切与命名

- 图像左上角为原点，列向右、行向下；文件行列编号从1开始，按先行后列排序。
- 每套外观使用独立目录，建议 `{主城}/{外观}/tiles/r01_c01.png`、`r01_c02.png`，依次编号。
- 正式图块各保留4096×4096的有效范围；用于衔接的外围上下文单独保留。重叠像素不得重复计入整图尺寸。
- 裁切清单记录主城、外观、版本、总行列数、整图像素宽高、文件名及每块在整图中的像素坐标和有效范围。
- 原生来源、完整提示词、参考图、重叠参数、拼接记录和文件校验值随批次保存。正式图块与参考裁图、过程稿分目录存放。
- 保存可重拼的清单与总览预览；总览可以缩小查看，正式图块保持无损4K。

## 4. 切图验收

1. 图块齐全，编号、坐标、行列与清单一致；每块均可解码且为4096×4096。
2. 按清单重拼后的尺寸与布局正确，没有缺块、重复块、错序或非等比拉伸。
3. 以100%像素检查所有横向、纵向接缝和四块交汇处，没有断路、错位、色带、亮暗跳变或模糊接缝。
4. 重点检查地砖、瓦片、栏杆、植被和建筑轮廓，确保真实细节清楚，没有放大糊感、锐化白边或噪点。
5. 在游戏最近镜头和跨块移动时核对清晰度与边界连续性。与地图相接的独立前景也应保持一致的画质、位置和轮廓。
6. 通过验收后交付正式图块、裁切清单、总览与来源记录；保留上一版和返工记录。

客户端分块加载、导入设置与接入验证另见[客户端说明](../mmorpg-client/Docs/CityTiles4K.md)；本轮模型、生成方案与进度另见[制作记录](qdao_city_tiles_4k_20260916/README.md)。
