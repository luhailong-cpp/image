> 2026-09-22 用户清理要求已执行。用户已明确授权删除旧原图和回退图。此要求覆盖本交接中早先的全部原图／失败版保留要求；当前仅保留最新选用图、未完区域必需小片、布局／材质设计和当前验收证据，历史来源 SHA 与文本记录保留。 详见[清理结果与当前检查方法](cleanup-current-assets/README.md)。原交接快照属于清理前历史，不再表示原图仍在磁盘。当前整城仍是 8／256 候选、正式验收 0。

# 主城美术详细交接：天墉城节庆 64K

交接原因：用户要求由新窗口继续制作。本窗口停止新增生图；旧原图和回退图已按用户后续授权清理，最新选用图、必要设计及来源文本记录保留。本文件是未完成制作的交接，不是完整地图的正式交付。

**截至本次交接：8／256 个坐标有 4096×4096 候选，248 个坐标尚无完整候选，正式验收 0 张，完整交付 0 套。** 第 8 排的原生小片没有拼成完整候选，不能加到这 8 张里。当前只集中制作 `tianyong_festival`，其他城市和外观保留。

## 1. 新窗口从哪里开始

工作目录：`D:\luyuan\wuxingqitan\image`。

先读以下文件，再核对实际文件和 SHA，不沿用旧交接中的 19 块进度：

1. `AGENTS.md`。
2. `主城地图切图规范.md`。
3. `qdao_city_tiles_4k_20260916/README.md`。
4. `docs/IMAGE_MODEL_POLICY.md`。
5. `config/image-generation.json`。
6. `D:/luyuan/wuxingqitan/mmorpg-client/Docs/CityTilePublishing.md`。
7. `designs/README.md`、项目 README 的接手说明与所指美术定调、`qdao_ui_redesign_v5/UI_SPEC.md` 第 2 节。
8. 本文件、下述实时状态及三个第 8 排目录各自的 `HANDOFF.md` / `handoff-state.json`。

本文件后文使用两种明确的目录简称：

- `ART` = `D:/luyuan/wuxingqitan/image/qdao_city_tiles_4k_20260916`。
- `SESSION` = `ART/builtin_q64_production/resume_single_city_20260921`。

主要入口：

| 内容 | 相对 ART 的路径 | 用法 |
| --- | --- | --- |
| 最新来源及完整候选计数 | `builtin_q64_production/resume_single_city_20260921/session-state.json` | 读取真实文件指针和 SHA；不把 `accepted=false` 改成通过 |
| 完整坐标与验收台账 | `builtin_q64_production/resume_single_city_20260921/current-coverage-ledger.json` | 256 坐标、480 相邻边、225 四块交点 |
| 本次交接冻结快照 | `builtin_q64_production/resume_single_city_20260921/handoff-20260921/snapshot.json` | 固定本次交接的来源 SHA 和分目录状态；新窗口继续后可能过时 |
| 8 张候选的坐标清单 | `builtin_q64_production/resume_single_city_20260921/handoff-20260921/candidate-coordinates.csv` | 文件、SHA、像素矩形、世界矩形；角色仍是候选 |
| 新窗口启动文字 | `builtin_q64_production/resume_single_city_20260921/handoff-20260921/新窗口启动提示词.md` | 整段复制给新窗口即可 |

制作根中的 `status.json`、`production_catalog.json`、`builtin_q64_production/current-batch.json` 通过 `activeProductionRun` 指向本 SESSION。旧候选数组及 24／502 是保留的接手基线，不能代替当前增量。旧 `E:/work/image` 对应本机 `D:/luyuan/wuxingqitan/image`；历史记录原样保留，读取时做路径映射，不批量改写历史。

## 2. 已有完整候选，选哪一版

旧 6 张统一取以下目录的 `output_v5`：

`ART/builtin_q64_production/tianyong_festival/upperpair_r09_c07_c08_row10_c07_c10_20260918/`

坐标是 `r09_c07`、`r09_c08`、`r10_c07`、`r10_c08`、`r10_c09`、`r10_c10`。对应来源为该目录 `assembly_v5.json`，旧局部检查为 `qa_v5/visual-review-20260920.json`。不要退回更早的同坐标图，也不要修改这些选用核心来迁就新图。

新增两张当前版本：

| 坐标 | 相对 SESSION 的候选路径 | PNG SHA-256 |
| --- | --- | --- |
| r09_c09 | `tools/repairs/versions/r09_c09_repair_v6/r09_c09.png` | `5ce3e9099c4292a3c2d2b854bcc6d2cfd3b8858bb6960bc8e7966699ade4742c` |
| r09_c10 | `next_tile_r09_c10/repairs/versions/external-v8/r09_c10.png` | `f5a45f15f69104c6f3dfe9f7a855e71f5d142d896cc78ffadbf12b3a57f813e4` |

`r09_c09` 的来源在相邻 `repair.json`，当前检查为 `SESSION/audit/r09_c09_review_v5/visual-review-v6.json`。`r09_c10` 的最新指针为 `SESSION/next_tile_r09_c10/latest-candidate.json`，检查为 `qa/external-v8/visual-review.json`。

这两张的“局部连续性通过”范围分别是内部接缝／交点、左边和下边全长接缝，以及实际西南四块交点。未变像素的检查可凭逐像素相等继承；改变区域重新看原像素证据。顶部、右侧未来邻居和其他尚未齐备的交点没有通过。局部通过不等于整城布局、导航、最近镜头或正式交付通过。

用于下一排底部上下文的最新扩展图：

- `r09_c09`：`SESSION/next_tile_r09_c10/references/latest-left-r09_c09-v6/extended-context.png`，SHA `e554f52f0fc3870301095bfdd3e60f66a07d92856785a30c8d1f73a2f5be0b32`。
- `r09_c10`：`SESSION/next_tile_r09_c10/repairs/versions/external-v8/extended-context.png`，SHA `a17c738a1c397cf1a18e8c9ec4994e0f87b8d9882a1bed8d3e5c9a6be3237b5b`。

全套候选文件的实际 SHA 和所有来源／QA 指针见本次快照及 CSV。预览见 `SESSION/previews/20260921T152330112878Z/candidate-contact-sheet-preview-only.png`，它只表示局部布局与画法，不是高清成品或原像素接缝证据。

## 3. 第 8 排未完工作：不要重做已保存原图

三个独立目录是 `SESSION/next_tile_r08_c07`、`next_tile_r08_c08`、`next_tile_r08_c09`。每块计划 4×4 共 16 个原生小片。**小片 ID 的 r01～r04 是图块内部坐标，不是整城图块坐标。**

本次停止时的精确选用数量、文件 SHA、失败版本、调用中断情况，以各目录 `HANDOFF.md` / `handoff-state.json` 和本文件末尾冻结摘要为准。`native/` 的 PNG 个数包括被替换版本，不能直接作为选用数量；有请求 JSON 而没有可靠返回 PNG，不算已完成。

| 整城坐标 | 续做入口 | 已知处理差异 |
| --- | --- | --- |
| r08_c07 | `make_tile.py`、`plan.json`、`requests/`、`native/*.record.json` | 从底排向上、每排从左到右；已选 v2 替换部分首版；原图和每次真实请求分别保留 |
| r08_c08 | `save_native.py`、`plan.json`、`native/*.generation.json`、`*.tool-response.json` | 同一坐标可有 v2；工具回执内含实际请求，保存脚本从真实 output_hint 找原文件 |
| r08_c09 | `native_tools.py`、`plan.json`、`requests/`、原图旁记录 | 最新材质参考改用 r09_c09 v6 的原像素裁切，避免整块缩略参考掩盖材质问题 |

不要无条件重跑 `init`、`prepare` 等初始化命令覆盖计划和引导；先读各目录交接给出的下一条命令。已有实际调用中断时，先检查原回执指向的缓存和本地保存原图，不猜测输出，也不把未知结果补成成功。

这三个目录均未完成完整 4K 接缝验收。接续顺序建议：先补全最接近完成的 r08_c07，再补 r08_c08、r08_c09；每块完成后检查与底排旧候选及相邻第 8 排的边界，再推进 r08_c10。可并行不相互依赖的原生片，共享边界与四块交点必须在相关版本固定后统一检查。

## 4. 当前有效的画法和返修经验

用户要求圆润饱满、明亮干净的道家 Q 版。新图实际附带 `designs` 已确认的成图作画法参考；本轮采用 `D:/luyuan/wuxingqitan/image/designs/guild-ui-v2/source/guild-overview.png`，SHA `85d0c8260237fb6381d6c54005d5f2ac9f4b065a16d318e3e12e6498312a40a6`。只借用画法、材质、完成度，不把 UI、文字、图标或新道具画进地图。

第 8 排初版出现过大面积碎片状、云斑状石纹、白色细脉，与干净材质不符。仅说“减少噪点”效果不稳定。有效提示明确区分参考用途：

- 第 1 张只决定几何、裁切、视角、真实大轮廓，不是材质权威；整片旧碎纹、裂纹、白脉、颗粒全部重画成宽缓而稀疏的色调。
- 以最新局部合格候选的 **原像素材质裁切** 为材质参考，保持浅象牙石、安静的蓝灰石面、克制的金边和圆润清楚的倒角。
- 引导图在 x=230、y=1024 等处可能有粘贴上下文的直线材质分界。这些是参考拼贴痕迹，不是地图结构，应跨越该处统一材质；真实砖缝、环形边和雕刻轮廓保留。
- 不添新砖缝、台阶、栏杆、植物、灯笼、角色或装饰；不改道路宽度、建筑位置、出入口和可行走区域。

可复用各目录已成功 v2 的实际提示词和输入顺序，但换坐标后必须重新准备正确几何参考，不能复用别块的轮廓。

已有失败问题的文本记录保留，相关旧图片已清理：r09_c09 早期白边错台和碎纹、v4 的外边材质硬线；r09_c10 external-v5/v6 的错台与底边重复砖缝；第 8 排 v1 的碎纹。**保留失败结论文本，不能把旧报告改成通过；无需保留旧失败 PNG。**

## 5. 原生制作与拼接规则

每个最终 4K 块由 16 张实际生成的 1254×1254 原图提供细节；核心 1024×1024，四周各 115 像素上下文，相邻原片重叠 230 像素。以 1024 间距组成 4326×4326 扩展图，再裁 `[115,115,4211,4211]` 得 4096×4096。制作中的选用小片保留完整返回字节，完成组装后可按用户要求清理原图；来源 SHA 和生成记录保留。

布局引导允许机械缩放，以帮助生成保持几何；引导、缩略图和预览均标明 reference/preview only，禁止把其放大像素放入最终来源链。当前机械拼接仅作重叠选择、局部配准和边缘色差处理；其位移、掩膜、色彩调整、来源和脚本 SHA 都要留记录。

`SESSION/tools/city_repair.py` 用于准备修补上下文并应用 AI 生成的局部原图。现有修补记录采用最高约 8 像素的有限配准与约 18 的色差限制；这些是已记录的处理参数，不是所有新图自动安全的许可。每次仍查掩膜外像素未改变、修补返回带、受影响的内部缝与交点。

相邻图块必须按真实核心的全局像素所有权取上下文。旧扩展图的 115 外圈可能属于另一历史版本，不能覆盖该坐标最新的真实 4096 核心。r09_c10 左下 230×230 上下文曾因此不一致，已按 r09_c09、r10_c09、r10_c10 的真实核心分别重建。后续交点延续这个方法，不把彼此矛盾的 halo 都当作权威。

## 6. 布局、导航与坐标

整城为 65536×65536，左上原点，像素 X 向右、Y 向下；r、c 从 1 起。图块像素矩形为 `[(c-1)*4096, (r-1)*4096, 4096, 4096]`。

世界范围 X=50～350、Z=0～300；单块 18.75 单位，矩形低 Z 边为 `z=300-r*18.75`，`x=50+(c-1)*18.75`。客户端 manifest 的 y 对应世界 Z。不要按 150×150 导航格取整图块边界：一块横跨 9.375 格。

布局权威与局部参考：

| 用途 | 文件 | SHA-256 |
| --- | --- | --- |
| 原整城布局 | `D:/luyuan/wuxingqitan/image/tianyong_festival_hd_20260910/tianyong_city_master_6144.png` | `aea4c03a216138cd447187279fb62d9f5660da875140a155ca01f88dd1f11428` |
| 局部广场候选参考 | `ART/builtin_4x4/output/tianyong_plaza_4k_candidate_v3b.png` | `21a40f32316a0d77e411510b71d8dc80a4ace8c4b0e4605291ee060b1aeccf98` |

全图最终像素 G 对应原 6144 图坐标 `S=G*3/32`；广场参考坐标 `P=G*3/16-4096`。广场名义上覆盖原图 `[2048,2048,4096,4096]`，但它是重绘候选，不是逐像素同几何变换。

`SESSION/tools/layout-source-audit.json` 证明旧客户端 36 张旧纹理与原 6144 来源、旧导航掩膜的静态绑定。此证据不能证明新 64K 画稿已通过导航。`SESSION/audit/layout-alignment-review/` 的实际图像对照发现大地标近似保留，同时铺装线、刻纹和边宽有重描；精确布局与导航仍待验。

`r09_c11` 右侧约 1365 个最终像素超出广场参考，涉及水岸、护栏、树木，必须回到原 6144 全城上下文校准，不能外推广场猜画。第 8 排 c07～c10 均有局部广场支持；其道路、建筑及出入口仍要与原图核对。

通用参考生成器为 `SESSION/tools/prepare_geometry_guides.py`。已准备 `SESSION/future_geometry_guides/r08_c10/`：16 张参考、24 条共享重叠逐像素相等检查、脚本快照、底邻 r09_c10 v8 绑定。该目录 **没有新增美术成品或原生细节**。对新坐标生成时工具会检查整张扩展参考是否越过广场，必要时改用原整城来源。

## 7. 型号证据：已确认与未确认

本批配置目标为 `gpt-image-2.5-sunburst` / `max`，路径为宿主内置 `image_gen.imagegen`。本次已核对官方文档及真实工具 schema，能力记录在 `SESSION/model-capability.json`。**实际后端是否 2.5 或 2.0、实际质量档位均未确认**；原始 PNG 的通用 ChatGPT / gpt-image 字符串也不能证明具体版本。

本轮工具只暴露 `prompt`、`referenced_image_paths`、`num_last_images_to_include`，没有 model/quality/size 选择器；返回没有可验证的具体型号。因此逐图 `actualModel=null`、`actualQuality=null`、`backendModelVerified=false`。新窗口必须重查入口能力；若仍不披露，继续如实记录未知，不凭配置或提示词宣称用了某型号。

每张图留配置快照、实际提交参数、实际请求及参考 SHA、完整输出及 SHA、真实响应、保存时间。服务器生成时刻不可得时 `generatedAt=null`，本机收到结果时间独立写 `observedCompletionAt`，不能混淆。旧图不因配置升级而改型号记录。

`SESSION/provenance/` 提供按内容 SHA 保存的补充旁证，旧文件不覆盖。历史 r09_c10 有 18 张细节／参考只有提示词、输入 SHA、toolCall 与真实工具响应，缺完整请求 JSON；这个限制已明确保留，没有伪造缺失请求。未使用付费 API/CLI；本交接不授权切换独立计费路径或索取密钥。

## 8. 本机续作和技术核验命令

以下命令在 image 工作目录执行，依次进行。它们不生图、不代表视觉验收，也不发布客户端：

```powershell
Set-Location 'D:\luyuan\wuxingqitan\image'
$CityArtPython = 'C:\Users\Administrator\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe'
$CityArtSession = 'qdao_city_tiles_4k_20260916\builtin_q64_production\resume_single_city_20260921'

# 原图/候选写入稳定后再刷新；先保存当前 session-state 和 ledger 作为回退。
& $CityArtPython "$CityArtSession\refresh_session.py"
# 旧 audit_native_sources.py 是历史全量原图审计；删除原图后不要再把它当当前全量通过检查。
& $CityArtPython "$CityArtSession\tools\verify_checkpoint.py"

# 上述检查无错误后，更新三个共享 JSON 的 activeProductionRun 指针。
# 脚本会保留原字节备份，并拒绝覆盖读后发生的并发变化。
& $CityArtPython "$CityArtSession\sync_active_checkpoint.py"
```

`refresh_session.py` 扫描新原图、返修记录和 `next_tile_*/latest-candidate.json`；只有真实存在并匹配 SHA 的 4096 候选才增加完整坐标。候选指针应写文件、SHA、尺寸、来源/QA、正式状态 false，并只列真正看过的 `reviewedNeighborTiles` / `reviewedJunctionIds`。不能为更新计数而提前造指针。

`verify_checkpoint.py` 完整解码当前候选和新增原生图，校验尺寸、SHA、已有记录指针、256/480/225 台账和世界坐标；读前后文件变化会报告 `STALE_FAIL`。`TECHNICAL_CONSISTENT` 只表示文件一致，不表示美术、导航或实机通过。每次技术报告和 provenance 索引均新增时间戳版本。

Pillow/numpy 使用上面的 bundled Python。修补辅助所需 cv2 已在 `SESSION/tools/vendor/` 隔离放置，依赖说明是 `tools/repair-requirements.txt`；不要为续作修改系统 Python 或清理该目录。通用参考工具和各块实际命令先读各自源码／交接，不盲目执行旧多外观轮转脚本。

## 9. 必须继续的验收

1. 补齐全部 256 坐标的原生细节。新增 4K 块逐片查清晰度，不能用整块缩略图掩盖低清或材质噪点。
2. 每块先看 6 条内部完整缝和 9 个内部四片交点，再看 4K 图块之间完整边界；返修后重看变化区域、返回带与受影响交点。
3. 整城共 240 条左右相邻边、240 条上下相邻边，合计 480 条；四块交点 225 个。台账完整不表示检查完成，缺邻居时保持 pending。
4. 原道路、建筑、出入口、出生点、固定视角、导航可行走区逐区域对照原整城；几何映射正确不能替代画面语义检查。
5. 最近镜头清晰度、导航和天墉旧前景兼容需要真实对应版本证据。客户端窗口负责接入和实机采证，本美术窗口不能自行填“实机通过”。

当前 8 张候选只有 10 条外边具备双方候选、3 个外部四块交点具备全部候选；本轮新记录的局部外边通过 4 条、交点通过 2 个。其余不能按“推测连续”标通过。旧六块的历史 QA 单独保留，不能把历史局部结论扩大为整城验收。

## 10. 最终交给客户端窗口的内容

以最新 `D:/luyuan/wuxingqitan/mmorpg-client/Docs/CityTilePublishing.md` 为准。完成一套就交这一套；不用等其他六种外观，也不在本窗口接入 Unity。

- 256 张真正合格的 4096×4096 PNG，`r01_c01` 至 `r16_c16`，行优先清单，无遗漏／重复。
- 每张文件 SHA、最终全图像素矩形和世界矩形、原生来源记录及 SHA、assembly 引用及 SHA、修补来源链。
- 对应同一 appearance/version、tileCount=256、tilesDigest 的真实美术验收记录，含审核人、时间、证据文件 SHA。
- 整城布局、横缝、纵缝、四块交点、最近镜头清晰度、导航对齐六项真实证据。
- 天墉还要求前景剪影、遮挡、脚点、光照、最近镜头五项证据，绑定实际受审客户端前景代码、旧纹理与 meta；此前合同审计按 75 个绑定文件统计，接续时按客户端最新合同和文件复核。

当前不得生成宣称 `purpose=production_city_tiles` / `status=accepted_complete` 的正式交付，不得把候选改名成 production_tile 绕过检查。客户端工具的 contract 输出仅是含失败项和占位值的草案。正式 digest/validate/stage/publish 由完整真实交付驱动，客户端窗口继续发布、Unity 导入和正式登录进城验收。

## 11. 并发、历史与回退

本窗口未执行 git add、commit、push 或 reset；已按用户后续授权删除清单中的过时主城图片及过程数据。其他窗口持续修改 AGENTS、策略、角色、客户端等文件，并发提交中可能包含本窗口早期 WIP；提交到仓库不等于美术验收。不要撤回或清理这些工作，不运行 git clean、reset、全目录覆盖或旧工作区路径上的删除。

历史生成／拼接／失败结论的文本与 SHA 记录仍在原处；旧原图、候选回退版和过时检查图片已按清理清单删除。保留的选用小片可继续制作，已删除来源无法逐字节重放。不要尝试从旧 E 盘路径恢复或把删除项当作通过复验。

根共享 JSON 更新前须保存原字节并核对读前后 SHA；有并发变化就重新读取合并。本次停止状态只说明移交新窗口，不是目标完成；新窗口继续后，应新建检查点并以新的真实文件记录为准。

## 12. 本次冻结快照与剩余小片

冻结记录：[snapshot.json](builtin_q64_production/resume_single_city_20260921/handoff-20260921/snapshot.json)。文件名按本机 2026-09-21 命名；末次核验的 UTC 时间已进入 2026-09-22，两者不矛盾。精确冻结时间在 JSON 的 `frozenAtUtc`。

| 目录 | 保留原生 PNG（含失败版） | 选用待拼接小片 | 缺失／需重绘 | 下一步 |
| --- | --- | --- | --- | --- |
| r08_c07 | 12 | 9／16 | 7 | 先核对 r02_c02、r01_c01 中断调用是否有可恢复真实结果，再补绘 |
| r08_c08 | 5 | 2／16 | 14 | r01_c03 旧版碎纹需重画，随后补其余 13 个缺失坐标 |
| r08_c09 | 9 | 5／16 | 11 | 从 r02_c02.v2 的已准备请求继续 |

分目录的可直接续做说明和逐小片 SHA：

- [r08_c07 HANDOFF](builtin_q64_production/resume_single_city_20260921/next_tile_r08_c07/HANDOFF.md) ／ [机器状态](builtin_q64_production/resume_single_city_20260921/next_tile_r08_c07/handoff-state.json)。选用底排 r04_c01～c03 的 v2、r04_c04、r03_c01～c04、r02_c01。
- [r08_c08 HANDOFF](builtin_q64_production/resume_single_city_20260921/next_tile_r08_c08/HANDOFF.md) ／ [机器状态](builtin_q64_production/resume_single_city_20260921/next_tile_r08_c08/handoff-state.json)。选用 r01_c01.v2、r01_c02.v2；plan 里旧 r01_c03 的“已保存”不能理解为材质合格，本次明确排除。
- [r08_c09 HANDOFF](builtin_q64_production/resume_single_city_20260921/next_tile_r08_c09/HANDOFF.md) ／ [机器状态](builtin_q64_production/resume_single_city_20260921/next_tile_r08_c09/handoff-state.json)。选用首排四张 v2 和 r02_c01.v2；plan 中其他 outputFile 是拟用路径，文件尚不存在。

上述“选用”只表示继续拼接时的当前版本，不表示未做过的视觉检查通过。原生来源本轮累计 60 张：49 张细节、10 张 AI 返修、1 张生成风格参考。其中本排 26 张包含被排除的 10 张旧版；全项目已知保留原生数（不含生成参考）为 565，全部外观完整候选合计仍是 26 个坐标。

最后技术检查：[报告](builtin_q64_production/resume_single_city_20260921/tools/checkpoint_reports/checkpoint-20260922T021558109325Z.md)／[JSON](builtin_q64_production/resume_single_city_20260921/tools/checkpoint_reports/checkpoint-20260922T021558109325Z.json)。完整解码 8 张候选和 60 张原生来源，核对 372 个输入、256 坐标、480 条邻边、225 个交点，错误 0、检查期间输入变更 0。结果为 `TECHNICAL_CONSISTENT`，美术、导航、运行时验收均未执行。此前检查器未识别 r08_c07 第 3 版记录的顶层 file/sha256 字段，出现过技术失败报告；已补充文件身份与 SHA 绑定读取后重跑，旧脚本与失败报告保留，未修改原图或把失败视觉报告改成通过。

最后来源旁证：[逐图索引](builtin_q64_production/resume_single_city_20260921/provenance/index-20260922T021331490126Z.md)／[JSON](builtin_q64_production/resume_single_city_20260921/provenance/index-20260922T021331490126Z.json)，覆盖 60 张原生输出，缺文件／SHA 错误 0、读期间变化 0。实际型号、实际质量和服务器生成时刻仍未确认。

停止情况：本窗口已停止三个本地制作执行者，未启动新的生成。执行者此前发生响应流中断，旧宿主生图调用是否仍在运行、是否已返回但未落盘无法确认，因此记录 `hostSideInFlightStatus=unknown`，不声称已经取消或完成。没有真实回执及已保存原图的请求不计入交付数量。新窗口接手时先核对，无法恢复则正常补绘并另留新记录。

可直接复制给新窗口：[新窗口启动提示词](builtin_q64_production/resume_single_city_20260921/handoff-20260921/新窗口启动提示词.md)。本次是制作工作交接，整套仍未完成。
