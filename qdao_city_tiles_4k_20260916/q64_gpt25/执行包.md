# GPT Image 2.5 / Q64 首块执行包（停用历史方案）

**本包已停用。** 用户因独立 API 额外收费，改用内置 GPT Image 2.0 路线（`builtin_image_gen`）。严格 GPT Image 2.5 与 API 凭证条件不再适用；本轮不调用单独计费 API，不需要 Key。活跃工作转到 [builtin_q64_r10_c07](E:/work/image/qdao_city_tiles_4k_20260916/builtin_q64_r10_c07/)，首块尚未完成。以下参数、命令和检查记录仅供历史追溯，不能当作当前执行授权。

交付目标保持不变：整城65536×65536；16×16个4096×4096交付块；每套256块。七套外观共计划1792块，计划数不代表完成量。

首块为天墉r10_c07，覆盖18.75×18.75世界单位，保留原导航范围和图像坐标。参考裁图只有布局用途，不能把放大的参考当作新增高清美术。

## 旧方案的参数与原始依据

- 明确模型：`gpt-image-2.5-sunburst-2026-09-08`。
- 质量：`max`。
- 原生请求：`2160x2160`，四张；每张2048核心，外围各56，上下左右相邻重叠112。拼为4208方图，外围各裁56，得4096方图。
- 2160²符合官方16像素步进、最大边长3840、最大面积8294400与1:3至3:1比例限制，但大于2560×1440的尺寸属于实验性范围，实际返回尺寸必须验证。
- 更Q版：圆润饱满、颜色明亮干净、细节结构清楚；减少写实裂纹与颗粒，保持地图布局及跨边界结构。

官方依据：[模型与max质量](https://developers.openai.com/api/docs/models/gpt-image-2.5-sunburst)、[尺寸限制](https://developers.openai.com/api/docs/guides/image-generation#size-and-quality-options)。

## 停用前执行记录

停用前，用户曾授权首块样图的 API 路线；现有 CLI 随后返回 `OPENAI_API_KEY not set`（退出码1），生成请求未发出。该凭证失败保留为历史记录，已不再阻塞当前内置路线，也无需为本轮配置凭证。详见[调用预检记录](qa/cli-preflight-missing-key.json)。历史内置图的 C2PA 生成动作记录为 `softwareAgent={name:gpt-image,version:2.0}`，未作密码学验签；内置没有型号选择器，新的输出仍须如实记录可获得的来源证据。

以下命令是停用 API 方案的历史原文，本轮不执行。保留命令仅用于追溯曾准备的四分区参数，不代表当前授权或后续执行步骤。

```powershell
gpt-image -p (Get-Content -Raw -LiteralPath 'E:\work\image\qdao_city_tiles_4k_20260916\q64_gpt25\prompts\r01_c01.prompt.txt') -i 'E:\work\image\qdao_city_tiles_4k_20260916\q64_gpt25\references\r01_c01.layout-only.png' -f 'E:\work\image\qdao_city_tiles_4k_20260916\q64_gpt25\native\r01_c01.png' --model gpt-image-2.5-sunburst-2026-09-08 --quality max --size 2160x2160 --format png --background opaque
gpt-image -p (Get-Content -Raw -LiteralPath 'E:\work\image\qdao_city_tiles_4k_20260916\q64_gpt25\prompts\r01_c02.prompt.txt') -i 'E:\work\image\qdao_city_tiles_4k_20260916\q64_gpt25\references\r01_c02.layout-only.png' -f 'E:\work\image\qdao_city_tiles_4k_20260916\q64_gpt25\native\r01_c02.png' --model gpt-image-2.5-sunburst-2026-09-08 --quality max --size 2160x2160 --format png --background opaque
gpt-image -p (Get-Content -Raw -LiteralPath 'E:\work\image\qdao_city_tiles_4k_20260916\q64_gpt25\prompts\r02_c01.prompt.txt') -i 'E:\work\image\qdao_city_tiles_4k_20260916\q64_gpt25\references\r02_c01.layout-only.png' -f 'E:\work\image\qdao_city_tiles_4k_20260916\q64_gpt25\native\r02_c01.png' --model gpt-image-2.5-sunburst-2026-09-08 --quality max --size 2160x2160 --format png --background opaque
gpt-image -p (Get-Content -Raw -LiteralPath 'E:\work\image\qdao_city_tiles_4k_20260916\q64_gpt25\prompts\r02_c02.prompt.txt') -i 'E:\work\image\qdao_city_tiles_4k_20260916\q64_gpt25\references\r02_c02.layout-only.png' -f 'E:\work\image\qdao_city_tiles_4k_20260916\q64_gpt25\native\r02_c02.png' --model gpt-image-2.5-sunburst-2026-09-08 --quality max --size 2160x2160 --format png --background opaque
```

逐张记录实际模型参数、质量、尺寸、生成路径、完整提示词与参考/输出SHA256及可获得的服务回包证据。不编造model回包或requestId。拼接后做原尺寸细节与接缝检查，再按真实游戏最近镜头验收；当前不发布生产manifest。

## 拼接与预检

[机械拼接脚本](assemble.py)已备好，不调用生成接口。已完成全尺寸合成坐标夹具检查：四张2160方图拼成4208，外围各裁56得到4096；方向、裁切、整数混合和PNG回读逐像素一致。检查结果见[机械预检](qa/mechanical-preflight.json)。这只证明拼接运算正确，不代表已生成真实美术或已通过接缝观感验收。

旧 API 方案原定在正式生成后，为每张原生图保存真实调用的来源记录；运行 `python assemble.py --record-contract` 查看记录格式。请求的模型参数与服务实际返回的模型信息分开保存；接口不返回型号时记录null，不编造回包。

旧 API 方案原定四张原生图及来源记录齐备后，运行 `python assemble.py` 输出首块候选及原像素接缝检查图，再运行 `python assemble.py --check` 复核。缺少生成证据时脚本拒绝创建成品。该旧 API 包未产生正式原生图；后续内置生成记录保存在新的活跃目录，不写入本包。