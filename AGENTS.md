# 项目绘图偏好

- 主城地图重绘、拼接、切图与客户端分块加载，先读 [Image 切图规范](Image切图.md)：每个正式图块 4096×4096，整图尺寸由最终行列决定；原生子块拼合、接缝、来源记录与发布验收按该规范执行。该入口不替代下文 UI 制作规则。

- 在本项目生成或编辑图片，每个新生图任务开始前核对 OpenAI 官方最新正式 GPT Image 型号，选择当前账号与调用入口可用的最高能力版本及其最高质量，不永久固定某个版本；同一批次沿用本次核对结果。用户明确另选模型或质量时遵从该次要求。执行细则见 [最新模型策略](docs/IMAGE_MODEL_POLICY.md)。
- 优先使用 ChatGPT/Codex 宿主内置 `image_gen`，无需单独配置 `OPENAI_API_KEY`。先核对宿主模型能力；开放选择器时，显式传入本次确认的真实模型 ID 和最高质量。当前已验证基线为 GPT Image 2.5 Sunburst（`gpt-image-2.5-sunburst`）、`quality=max`，新正式版本可用时按上述规则升级。
- 强制执行最新版核对、显式选择与禁止静默降级。内置工具未开放模型选择器时，提示词不能代替后端切换：核对官方说明及运行时证据；明确仍为旧型号时，不把该路径当作满足最新版要求继续生成；无法确认时如实说明模型由宿主管理、最新版未获验证。需要严格锁定型号时，仅使用已获授权且支持显式模型参数的路径。参考图本地读取失败时优先排查输入通道或使用工具支持的对话图片；文件故障与账号／密钥配置分开诊断。
- API/CLI 是用户明确选择的备用路径。使用现有 `gpt-image` 工具，显式传入本次核对的 `--model` 与最高 `--quality`，覆盖旧的本地默认值；当前基线示例为 `--model gpt-image-2.5-sunburst --quality max`。不得臆造 `latest` 型号别名。单独计费路径须有用户授权，凭证只从本机安全配置读取；已有备用授权不改变内置优先规则。
- 换电脑时将本文件随项目一起同步，在新环境确认内置生图工具或相应 CLI、账号权限可用；本文件保存项目偏好，不负责同步账号、凭证和用户目录工具。安装细节见 [工具移机说明](docs/AI_DESIGN_TOOLS_SETUP.md)。
- 制作游戏素材前，阅读 [README 的接手说明](README.md#给接手的-codex) 指向的交接与美术定调文档。
- 制作、修改、实现 UI 或重新切图，以及收到其他游戏截图时，先读 [UI 制作规范第 2 节](qdao_ui_redesign_v5/UI_SPEC.md#2-统一视觉与控件层级)：按指定风格图和提示词执行，保留道家 Q 版与春节／元宵／中秋点缀；其他游戏截图默认只参考功能，不改变美术风格。
